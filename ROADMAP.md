# Roadmap & Changelog

Progress log for the Eric So portfolio (`ccso.shsl.world`). Timestamps in **UTC+8 (Asia/Hong_Kong)**, newest first.

Scope is frozen at **v1.0** — rationale in [`docs/DECISIONS.md`](docs/DECISIONS.md). The v1.0 scope defines Phases 1–7; this file tracks what has actually shipped.

---

## What we've done

### 2026-09-27

- **02:26 — M4-1 pure-evidence positioning hygiene + desktop refinement — landed** — technical gate met at 02:06 HK on `main` run 36261240380 (the first production deployment carrying the change: `verify` → `e2e` → `deploy (production)`, 16 files uploaded, Version `b2e6e5aa…`, readiness 1/24 → 200, smoke 11/11) and 02:08 HK (live read-back of every route); the landing record landed at 02:26 via this pull request. **Owner AUTH (2026-09-27): "M4-1 — Pure-Evidence Positioning Hygiene + Desktop UI/UX Refinement" — one coherent goal: make the existing production shell read as a finished pure evidence portfolio on desktop, without redesigning the site or adding substantive case / About content; visual direction frozen; a pre-mutation review required (file-level diff plan, measured typography / colour tokens and contrast pairs, rejected / deferred screenshot-review items, no-claims check); stop after M4-1.** [PR #48](https://github.com/F-e-u-e-r/portfolio/pull/48) (`content/m4-1-positioning-hygiene-desktop-refinement`, commit `6ffcca9` on clean `main` `245d4f6`; pull-request run 36261038682 `verify` + `e2e` green; `mergeStateStatus` CLEAN; merged 02:04 with a merge commit `d9d6ea2`, no bypass): 18 files, +161 / −121 — positioning / IA: the homepage Writing teaser ("To be launched" / "Planned themes →") removed and the page ends on one full-width Lab evidence block (`src/pages/index.astro`); public navigation = Case Studies · Journey · Lab · Contact as one list in `src/data/site.ts` rendered by the header, the mobile menu and the footer (About unlinked while `/about/` is a placeholder, Writing and Topics unlinked, every "Soon" label and its CSS gone); `/internal-builds/` deleted (page + `PLACEHOLDER_ROUTES` entry → the normal 404 contract; it was never in the sitemap); `/writing/` no longer promises a first article (the `firstItem` prop removed from `PlaceholderPage`); Contact neutral utility only (h1 "Contact", factual lead, Email + GitHub / Feuer; the `resume` field and every TODO framing removed, `linkedin` optional and silent while empty); `docs/DECISIONS.md` gains the positioning / IA entry; README's route table drops the deleted route. Desktop refinement, measured before and after (computed CSS + WCAG contrast at 1440×900, 1280×800 and 1920×1080): card descriptions 14 → 15 px (`--text-md`); `--c-border` 0.12 → 0.16 alpha (surface vs page measured at 1.10:1, edge 1.20 → 1.31:1); `--c-accent-muted` (`#27a97c`, ≥ 6:1) for eyebrows / spine years / principle numbers / TL;DR labels while links keep the accent plus a hover / focus underline and primary fills stay the strongest layer; full-height page shell (`body` flex column, `main` grows — `/contact/` had a 169 px dead band under the footer at 1920×1080 and `/about/` 68 px, now 0 on every route, long pages scroll as before); the "In preparation" tag 9.9 → 12 px; colours unchanged (every text colour already ≥ 5.2:1 AA), fonts unchanged (the declared stack renders system-ui / Menlo by design — no loading defect), container width, section spacing and the card language unchanged. Preflight (read-only, 01:30–01:45): local = origin = `245d4f6`, 0 open pull requests; variables `PRODUCTION_URL=https://ccso.shsl.world`, `PUBLIC_SITE_INDEXABLE=true`, `CLOUDFLARE_DEPLOY_ENABLED=true`, `CLOUDFLARE_PREVIEW_ENABLED` absent; live smoke 11/11 and eight live routes byte-identical to the baseline build; baseline gates green on `245d4f6` (`astro check` 0 errors · build 15 pages · `verify-dist` 549/549 launch · `test:tools` 25/25 · Playwright 20 passed / 2 skipped). The pre-mutation review was shown before any mutation — diff plan, measured tokens and contrast pairs, the rejected / deferred list, and the claim check: no title, employer, metric, achievement or experience is introduced and no content file changes. Local gates on `6ffcca9`: `astro check` 0 errors · build 14 pages · `verify-dist` 412/412 in launch and pre-launch mode · `test:tools` 25/25 · Playwright 29 passed / 3 skipped with the spec rewritten from the resulting IA (header 4 sections, mobile menu 4 + GitHub with no Soon, no unlinked route in the chrome, `/internal-builds/` → 404, placeholders promise no item, Contact neutral, short-page shell at 1280×1400, long pages still scroll) · known-bad arm: the new spec fails 13 assertions against the previous dist · no residue string ("Soon", "To be launched", "Planned themes", "First article", "Get in touch", "fastest route", "View Resume", "internal-builds") in any built page. `main` run 36261240380 (02:04–02:06): Deploy uploaded 16 files (7 already uploaded), Version `b2e6e5aa…`, "No targets deployed" (Custom Domain Dashboard-managed, as since M3D), readiness 1/24 → 200, smoke 11/11. Live read-back 02:08: `/`, `/case-studies/`, `/journey/`, `/lab/`, `/about/`, `/contact/`, `/writing/`, `/topics/`, two case pages and `/under-maintenance/` byte-identical to the artifact (the merge tree equals `6ffcca9`); `/internal-builds/` 404 for bare and navigation clients with the `ROUTE_NOT_FOUND` page; `/about/`, `/writing/`, `/topics/` 200 + `noindex, nofollow`; `robots.txt` (318 B) and `sitemap-0.xml` (9 URLs) identical to the build; zero residue strings on the live pages; header and footer navigation = Case Studies · Journey · Lab · Contact; edge baseline unchanged (http → https 301 with path + query kept, POST 403, TLS 1.1 rejected, port 8443 403, `workers.dev` 404); the live site measured at 1920×1080 ends `/about/` and `/contact/` with the footer at the viewport bottom (dead band 0). **Acceptance:** Home has no Writing launch promise and still states Business Transformation & Applied AI ✓ · Home ends on existing evidence, not a placeholder ✓ · no "Soon" in any public chrome ✓ · About no longer in the navigation while `/about/` stays `noindex, nofollow` ✓ · `/writing/` and `/topics/` `noindex, nofollow`, unlinked, no first-article promise ✓ · `/internal-builds/` → the normal 404 contract, absent from the sitemap ✓ · Contact = Email + GitHub only, no public CV / LinkedIn obligation implied ✓ · Case Studies / Journey / Lab substantive content unchanged ✓ · secondary / card text measurably larger where changed, short pages on the full-height shell ✓ · responsive tests pass ✓ · indexability, crawler policy, canonical behaviour, sitemap, the M3C-1.5 baseline and the Cloudflare architecture unchanged ✓. Nothing else changed: no Cloudflare, GitHub-configuration, Search Console or public-repository mutation. Next: the M4 case-content cycles (M4-2 Worldview Council + StarLedger → M4-3 Sales-to-Ops → M4-4 HK Employment RAG → M4-5 About short version + `/about/` live with its navigation item restored → M4-6 baseline gate + record) — each separate AUTH; M4-0b is superseded by the positioning decision (LinkedIn and a public resume are not v1 requirements); Writing returns only with published articles, Topics stay deferred.

### 2026-09-26

- **20:08 — M4-0a public-baseline hygiene — landed** — technical gate met at 19:52 HK on `main` run 36240090290 (the first production deployment carrying the change: `verify` → `e2e` → `deploy (production)`, readiness 1/24 → 200, smoke 11/11) and 19:55–19:58 HK (live read-back of Home / Journey / Contact); the landing record landed at 20:08 via this pull request. **Owner AUTH (2026-09-26, after the read-only M4 scope review): "M4-0 public-baseline hygiene" — four items; the owner descoped this cycle to items 2–3 because the LinkedIn URL is not recorded anywhere reachable (repository, GitHub profile / social accounts, public code); item 1 (the LinkedIn link) and item 4 (the homepage Writing teaser pointing at LinkedIn) wait for the URL as M4-0b, separate AUTH.** [PR #46](https://github.com/F-e-u-e-r/portfolio/pull/46) (`content/m4-0a-public-baseline-hygiene`, commit `710b623` on clean `main` `c683a09`; pull-request run 36240002981 `verify` + `e2e` green; `mergeStateStatus` CLEAN; merged 19:50 with a merge commit `93ff043`, no bypass): 3 files, +19 / −12 — `src/pages/contact/index.astro` renders only links with a real destination (the shared footer's rule: the disabled "LinkedIn — Coming soon" and "Resume — View Resume" rows are gone and return once `contact.linkedin` / `contact.resume` are set in `src/data/site.ts`; no resume file published — none approved; description "email and GitHub (Feuer)"), `src/pages/journey/index.astro` drops the public development note "(Auto-pan / interaction: Phase 3.)", `docs/DECISIONS.md` gains the M4-0a entry and an updated Open / deferred list. Preflight (read-only, ~18:10–18:30): local = origin = `c683a09`, 0 open pull requests; variables `PRODUCTION_URL=https://ccso.shsl.world`, `PUBLIC_SITE_INDEXABLE=true`, `CLOUDFLARE_DEPLOY_ENABLED=true`, `CLOUDFLARE_PREVIEW_ENABLED` absent; live smoke 11/11; baseline gates green on `c683a09` (`astro check` 0 errors · build 15 pages · `verify-dist` 549/549 launch · `test:tools` 25/25 · Playwright 20 passed / 2 skipped). The proposed diff was shown before any mutation with the claim check: no title, employer, metric or credential is introduced — text is only removed. Local gates on `710b623`: identical results, and the built `dist/` differs from the `c683a09` build only in `contact/index.html` (three description tags + the two rows) and `journey/index.html` (the note). `main` run 36240090290 (19:50–19:52): Deploy uploaded 2 new assets (22 already uploaded), Version `cd415cf1…`, "No targets deployed" (Custom Domain Dashboard-managed, as since M3D), readiness 1/24 → 200, smoke 11/11. Live read-back 19:55–19:58: `/`, `/journey/`, `/contact/` byte-identical to the artifact; `/contact/` shows two rows (Email, GitHub / Feuer) and no "Coming soon" / "View Resume"; `/journey/` has no "Auto-pan"; `robots.txt` (318 B) and `sitemap-0.xml` (9 URLs) identical to the build; `/writing/`, `/topics/`, `/internal-builds/`, `/about/` 200 + `noindex, nofollow`; edge baseline spot checks unchanged (http → https 301 with path + query kept, POST 403, TLS 1.1 rejected, `workers.dev` 404). **Acceptance:** no dead Resume item ✓ · no LinkedIn "Coming soon" ✓ (the LinkedIn link itself is M4-0b) · no visible Phase 3 note ✓ · Writing / Topics / Internal Builds still deferred placeholders ✓ · indexability / sitemap / crawler policy / M3 security baseline unchanged ✓ · **not met in this cycle, by the owner's descoping: the homepage Writing teaser still reads "To be launched" (M4-0b)**. Nothing else changed: no Cloudflare, GitHub-configuration, Search Console or public-repository mutation. Next: M4-0b (LinkedIn URL + Writing teaser, once the URL is provided) → the M4 case-content cycles — each separate AUTH.
- **17:24 — M3D `workers.dev` retirement — COMPLETE record landed · M3 — custom-domain production cutover — COMPLETE** — technical acceptance gate met at 17:11 HK on `main` run 36231935602 (the first deployment with `workers_dev = false`) and 17:15 HK (retirement acceptance from the deployed artifact and the control plane); the completion record landed at 17:24 via this pull request. **Cycle:** preflight on clean `main` `6eb3bd5` (16:45–16:47, read-only: 0 open pull requests; `PRODUCTION_URL=https://ccso.shsl.world`, `PUBLIC_SITE_INDEXABLE=true`, `CLOUDFLARE_PREVIEW_ENABLED` absent; `workers_dev = true`, `preview_urls = false`; account `{enabled:true, previews_enabled:false}`; ccso launch acceptance 110/110 and smoke 11/11; `workers.dev` still serving the launch artifact = the pre-mutation control; G1–G4 healthy; DNS / Google Workspace baseline identical; `pr-27` not probed). [PR #44](https://github.com/F-e-u-e-r/portfolio/pull/44) (`config/m3d-workers-dev-retirement`, `2a2ee08`) changed exactly one line, `workers_dev = true` → `workers_dev = false` (`preview_urls = false` kept; no `[[routes]]` — the Custom Domain stays Dashboard-managed under the M3B least-privilege exception), validated locally (`astro check` 0 / 0, launch build → `verify-dist` 549/549, `test:tools` 25/25, token-less `wrangler deploy --dry-run` exit 0, Playwright 20 passed / 2 skipped) and merged 17:09:51 HK with merge commit `a01ea85` after `verify` + `e2e` SUCCESS and `preview` / `deploy (production)` SKIPPED on the pull request (run 36231877759), CLEAN, no bypass. **Production (`main` run 36231935602, 17:09:53–17:11:32):** `verify` → `e2e` → `deploy (production)` SUCCESS — Wrangler uploaded no assets (no site change) and reported "No targets deployed for ccso-portfolio" where every earlier deploy had printed the `workers.dev` trigger URL; Version `5a3d85f0…`; readiness 1/24 → 200 on `https://ccso.shsl.world`; production smoke 11/11 (homepage `index, follow`); no permission error, the CI token unchanged. **Retirement acceptance (17:12–17:15, read-only):** control plane `subdomain.enabled = false` on both the Worker and the script subdomain API, `previews_enabled = false`, 0 Previews; `https://ccso-portfolio.secc-biz.workers.dev` no longer serves the portfolio — `/`, `/about/`, `/sitemap-index.xml` and an unknown route answer HTTP 404 `text/plain` `error code: 1042` (`server: cloudflare`, identical with cache-busting), and `/robots.txt` answers Cloudflare's own platform "Content Signals" robots.txt (200, comment-only, no crawler groups, byte-different from the deployed file) — exactly the behaviour of a never-deployed name on the same subdomain (control probed); statuses are recorded, not asserted as a fixed contract (Cloudflare documents only "route disabled"). `https://ccso.shsl.world` unchanged and now the sole intended public production hostname: 200, TLS valid (Google Trust Services WE1, to 2026-12-24), smoke 11/11, every sitemap route `index, follow` with self-canonical on `https://ccso.shsl.world/…` (9 URLs at this build — evidence, not contract), `/about/`, `/writing/`, `/topics/`, `/internal-builds/`, `/under-maintenance/` `noindex, nofollow`, 404 `noindex` without canonical, `robots.txt` = the six crawler groups + exactly one `Sitemap: https://ccso.shsl.world/sitemap-index.xml`, sitemap files byte-identical to the artifact and live-routes-only; G1 redirect chains, Minimum TLS 1.2, method WAF, port WAF, Bot Preference Sync off, no rate-limit rule — all unchanged; authoritative DNS / Google Workspace baseline identical to the M3B snapshot; `PUBLIC_SITE_INDEXABLE=true`, `PRODUCTION_URL=https://ccso.shsl.world`, `preview_urls = false`, `CLOUDFLARE_PREVIEW_ENABLED` absent. **Explicitly not claimed:** disabling the production `workers.dev` route does not disable Cloudflare Version URLs, Preview URLs or Deployment URLs (Cloudflare docs); Preview URLs stay disabled by `preview_urls = false` + the account flag; the deleted-Preview alias / immutable Deployment URLs of [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73) remain an independent, OPEN provider line. State: **M3D — `workers.dev` retirement — COMPLETE · M3 — custom-domain production cutover — COMPLETE** (public architecture: `ccso.shsl.world` = production endpoint, canonical hostname, indexable live routes, WAF / TLS baseline, sitemap + crawler policy; `workers.dev` production route OFF; Preview URLs OFF). Next: M4 content (About, cases, Journey) → Writing → Topics → launch polish; Search Console submission and the Public Repo Final Consistency Pass are separate AUTHs.
- **16:32 — M3C-2 public indexability cutover — COMPLETE record landed** — technical acceptance gate met at 16:23 HK on `main` run 36229515580 (the first production deployment built in launch mode) and 16:25 HK (public-indexability acceptance from the deployed artifact on both hosts); the completion record landed at 16:32 via this pull request. **Cycle:** Step A (the six-group AI-crawler policy, PR #40 / `8ba42f1`, record PR #41 / `dc24a27`) had already landed; repository variable `PUBLIC_SITE_INDEXABLE=true` set at 16:16 HK (read back exactly; the only mutation — no other variable, secret, Cloudflare setting, DNS, Custom Domain, `workers_dev`, CI, Wrangler, redirect, TLS, WAF, rate-limit or bot-setting change); start / cutover record [PR #42](https://github.com/F-e-u-e-r/portfolio/pull/42) (`docs/m3c2-step-b-indexability-cutover`, `fa215b7`; pull-request run 36229306876 with `verify` + `e2e` SUCCESS in launch mode — `verify-dist` "mode: launch (PUBLIC_SITE_INDEXABLE=true)" 549 checks — and `preview` / `deploy (production)` SKIPPED; CLEAN; merged 16:21:05 HK with merge commit `a53ba51`, no bypass). **Production (`main` run 36229515580, head `a53ba51`, 16:21:07–16:23:01):** `verify` → `e2e` → `deploy (production)` SUCCESS — Wrangler 4.137.0 uploaded 16 new or modified static assets (the 15 HTML pages + `robots.txt`; 8 unchanged); Version `62101731-f7db-4df4-966e-75a0b9d6d2d5`, deployment `355306bb…`; readiness 1/24 → 200 on `https://ccso.shsl.world`; production smoke 11/11 with the homepage `index, follow`. **Public-indexability acceptance (16:25, anonymous, derived from the deployed artifact `dist-a53ba51…` — byte-identical to the local launch rehearsal — not hardcoded):** on `https://ccso.shsl.world` every sitemap URL (9 at this build — evidence, not contract: `/`, `/case-studies/`, the four case studies, `/journey/`, `/lab/`, `/contact/`) answers 200 byte-identical to the artifact with `index, follow` and a self-canonical on `https://ccso.shsl.world/…`; every other page (`/about/`, `/writing/`, `/topics/`, `/internal-builds/`, `/under-maintenance/`) 200 `noindex, nofollow` with canonical on `https://ccso.shsl.world/…`; 404 (navigation) `noindex`, no canonical, body = `dist/404.html`; `robots.txt` = the six Step A groups plus exactly one final `Sitemap: https://ccso.shsl.world/sitemap-index.xml`, asserted group-for-group on the live body and byte-identical to the artifact (318 B observed, evidence only); `sitemap-index.xml` / `sitemap-0.xml` byte-identical to the artifact, listing only live routes (no placeholder / maintenance / 404 route); smoke 11/11; 24/24 artifact files byte-identical; TLS valid (Google Trust Services WE1, to 2026-12-24). `https://ccso-portfolio.secc-biz.workers.dev` (rollback host) serves the same launch artifact — therefore temporarily indexable too — with every canonical pointing at `https://ccso.shsl.world/…` and the same `robots.txt` / sitemap (110 acceptance checks ok per host); it stays live only until M3D. **Unchanged (read-only reconfirmation):** G1 HTTP→HTTPS chains (ccso / www / apex, path + query preserved, one hop, no loop), Minimum TLS 1.2 (1.0 / 1.1 rejected, 1.2 / 1.3 accepted), method WAF (non-GET/HEAD → 403 block page, `/cdn-cgi/rum` not blocked), port WAF (non-80/443 → 403; 80 → redirect; 443 → production), Bot Preference Sync off (`robots.txt` = build), no rate-limit rule, Cloudflare `{enabled:true, previews_enabled:false}` and 0 Previews, `PRODUCTION_URL=https://ccso.shsl.world`, `PUBLIC_SITE_INDEXABLE=true`, `CLOUDFLARE_PREVIEW_ENABLED` absent, authoritative DNS / Google Workspace baseline identical to the M3B snapshot (SOA serial 2415873814, unchanged since Step A). No claim is made about a verified Search Console property. State: **M3C-2 — public indexability cutover — COMPLETE** · M3 not complete. Next: **M3D — `workers.dev` retirement (`workers_dev = false`; separate AUTH)**.
- **16:21 — M3C-2 Step B started: public indexability cutover — repository variable `PUBLIC_SITE_INDEXABLE=true` set at 16:16 HK; the merge of this pull request is the first push to `main` built in launch mode** — preflight on clean `main` `dc24a27` (PR #41 merged, local = origin, 0 open pull requests; 16:09–16:12 HK, read-only): `PRODUCTION_URL=https://ccso.shsl.world`, `PUBLIC_SITE_INDEXABLE` absent, `CLOUDFLARE_PREVIEW_ENABLED` absent, `workers_dev = true`, `preview_urls = false`; both hosts smoke 11/11 (pre-launch), 24/24 files of the CI artifact `dist-dc24a27…` byte-identical, live `robots.txt` = the six Step A groups with no Sitemap line (asserted group-for-group), substantive pages `noindex, nofollow`, M3C-1.5 G1–G4 healthy (redirect chains with path + query preserved, TLS 1.0 / 1.1 rejected and 1.2 / 1.3 accepted, method + port WAF blocks, `/cdn-cgi/rum` not blocked), Bot Preference Sync off (`robots.txt` = build), no rate-limit rule, Cloudflare `{enabled:true, previews_enabled:false}`, authoritative DNS / Google Workspace baseline identical to the M3B snapshot. Launch-mode rehearsal on the same commit (16:11 HK, local): `astro build` 15 pages → `verify-dist` launch 549/549; `robots.txt` = the six groups + `Sitemap: https://ccso.shsl.world/sitemap-index.xml`; sitemap 9 URLs (count is evidence, not contract), each `index, follow` with self-canonical (`/`, `/case-studies/` + 4 case studies, `/journey/`, `/lab/`, `/contact/`); `/about/`, `/writing/`, `/topics/`, `/internal-builds/`, `/under-maintenance/` `noindex, nofollow` and absent from the sitemap; 404 `noindex` without canonical; Playwright 20 passed / 2 skipped against `wrangler dev` serving the launch build. Mutation: only `PUBLIC_SITE_INDEXABLE` set (read back exactly `true`); every other variable, every secret, the crawler policy, Cloudflare security baseline, DNS, Custom Domain, `workers_dev`, CI, Wrangler, redirects, TLS, WAF, rate limiting and bot settings unchanged. Expected on `main`: `verify` (launch-mode build + dist gate) → `e2e` → `deploy (production)` with readiness + production smoke (`index, follow` on the homepage) against `https://ccso.shsl.world`. M3C-2 is recorded COMPLETE only after the public-indexability acceptance from the deployed artifact passes on both hosts; `workers.dev` stays live as the rollback host and becomes temporarily indexable (canonical → `https://ccso.shsl.world/…`) until M3D.
- **15:53 — M3C-2 Step A robots and AI-crawler content-use policy — landed** — technical gate met at 15:37 HK on `main` run 36227263302 (the first production deployment carrying the six-group `robots.txt`) and 15:39 HK (dual-host read-back); the landing record landed at 15:53 via this pull request. **Owner decision (2026-09-26): search / discovery allowed, general model-training reuse disallowed — `Google-Extended` = disallow.** [PR #40](https://github.com/F-e-u-e-r/portfolio/pull/40) (`feat/m3c2-robots-ai-crawler-policy`, commit `8aa2105` on clean `main` `91c91f6`; pull-request run 36227191601 `verify` + `e2e` SUCCESS, `preview` / `deploy (production)` SKIPPED; merged 15:35:50 HK with merge commit `8ba42f1`, no bypass) changed three files together because the generator and the build gate must not drift: `src/pages/robots.txt.ts` now emits, in this order, `User-agent: *` / `Allow: /` / `Disallow: /cdn-cgi/`; `User-agent: OAI-SearchBot` / `Allow: /` / `Disallow: /cdn-cgi/`; `User-agent: Claude-SearchBot` / `Allow: /` / `Disallow: /cdn-cgi/`; `User-agent: GPTBot` / `Disallow: /`; `User-agent: ClaudeBot` / `Disallow: /`; `User-agent: Google-Extended` / `Disallow: /`, plus `Sitemap: https://ccso.shsl.world/sitemap-index.xml` only when `PUBLIC_SITE_INDEXABLE` is exactly `true`; `scripts/verify-dist.mjs` replaces the "must not mention `Google-Extended`" prohibition with an exact, ordered group-for-group comparison (every policy group, nothing else, Sitemap only in launch mode); `docs/DECISIONS.md` gains "2026-09-26 — AI-crawler content-use policy: `Google-Extended` disallowed". The durable contract is the exact groups / directives and the conditional Sitemap line; the observed file sizes (266 B pre-launch, 318 B launch) are evidence only. **Local validation before push (15:30–15:33):** `astro check` 0 errors / 0 warnings; pre-launch build → `verify-dist` 549/549; launch build (`PUBLIC_SITE_INDEXABLE=true`) → `verify-dist` 549/549; `test:tools` 25/25; Playwright 20 passed / 2 skipped against `wrangler dev`; `diff -r` against a clean build of `main` `91c91f6` in both modes → `robots.txt` the only differing file (24 files each), so the placeholder / maintenance / 404 indexability contracts are byte-identical (`/about/`, `/writing/`, `/topics/`, `/under-maintenance/` `noindex, nofollow` and `404.html` `noindex` in both modes; `/` and `/case-studies/` follow the mode; sitemap identical, 9 URLs, `/about/` absent); the previous gate rejects the new `robots.txt` and the new gate rejects the previous one; the new gate fails on each of 8 known-bad mutations and passes the unmodified builds. **Production (`main` run 36227263302, head `8ba42f1`, 15:35:52–15:37:44):** `verify` → `e2e` → `deploy (production)` SUCCESS — Wrangler 4.137.0 found exactly one new or modified static asset (23 already uploaded), Version `45fa6168-938a-43c1-b8ca-a01c7536629a`, deployment `9ca40c79…`; readiness 1/24 → 200 on `https://ccso.shsl.world`; production smoke 11/11. **Read-back (15:39, anonymous, both hosts):** `https://ccso.shsl.world/robots.txt` and the `workers.dev` rollback host answer 200 `text/plain` with the six-group pre-launch policy byte-for-byte equal to the CI artifact `dist-8ba42f1…` and to the local pre-launch build, asserted group-for-group on the live body — generic crawlers (`*`) allowed except `/cdn-cgi/`; `OAI-SearchBot` allowed except `/cdn-cgi/`; `Claude-SearchBot` allowed except `/cdn-cgi/`; `GPTBot` disallowed entirely; `ClaudeBot` disallowed entirely; `Google-Extended` → `Disallow: /`; no `Sitemap:` line (`PUBLIC_SITE_INDEXABLE` still absent); a cache-busted re-read identical; pages `noindex, nofollow` (`/`, `/about/`, `/under-maintenance/`); smoke 11/11 on both hosts; 24/24 artifact files byte-identical on both; canonical `https://ccso.shsl.world/`; TLS valid. **M3C-1.5 security baseline unchanged:** G1 redirect chains (ccso / www / apex, path + query preserved, one hop, no loop), G2 (TLS 1.0 / 1.1 rejected, 1.2 / 1.3 accepted), G3 + G4 (non-GET/HEAD and non-80/443 ports blocked = 403 WAF block page, `/cdn-cgi/rum` not blocked, `www` + `workers.dev` controls unchanged), no rate limiting, Bot Preference Sync off (`robots.txt` = build); Cloudflare `{enabled:true, previews_enabled:false}`, Previews 0; repository variables unchanged (`PRODUCTION_URL` = `https://ccso.shsl.world`, `CLOUDFLARE_DEPLOY_ENABLED=true`, `PUBLIC_SITE_INDEXABLE` absent, `CLOUDFLARE_PREVIEW_ENABLED` absent); authoritative DNS / Google Workspace baseline identical to the M3B snapshot (SOA serial advanced to 2415873814 with every audited record identical — observed only; the first DNS read at 15:40 was invalidated by an evidence-tooling file-name collision and re-run clean at 15:43). Not changed in this cycle: `PUBLIC_SITE_INDEXABLE`, Cloudflare (WAF / TLS / redirects / DNS / bot settings / Custom Domain / Access), `workers_dev`, CI, Wrangler, secrets. No claim is made about a verified Search Console property. State: **M3C-2 Step A COMPLETE** · M3C-2 not complete · M3 not complete. Next: **M3C-2 Step B — `PUBLIC_SITE_INDEXABLE=true` public indexability cutover (separate AUTH)** → M3D `workers.dev` retirement.
- **04:12 — M3C-1.5 transport and WAF security baseline — COMPLETE record landed** — technical acceptance gate met at 03:48 HK (G2–G4 read-only acceptance) after the G1 redirect acceptance at 03:39 HK; both WAF rules re-validated at 03:56 HK after the owner reordered them; the completion record landed at 04:12 via this pull request. **Read-only assessment (02:47–03:02; zone `shsl.world`, Free plan; the credentials on the operator machine cannot read WAF, zone-setting, bot or firewall-event state, so findings come from live edge behaviour, zone HTTP analytics and Cloudflare's documentation)** identified: plain HTTP served on `ccso.shsl.world` with no redirect and no HSTS (real browser sessions observed over http); Minimum TLS at Cloudflare's default 1.0 while every observed legitimate connection used TLS 1.2 / 1.3; a static-site method surface on which non-GET/HEAD requests reached the Static Assets edge (405) while internet scanners were POSTing to `/api` and `/graphql`; and the application answering on Cloudflare's alternate HTTPS ports (8443, 2053, …). Already in place: HTTP DDoS protection (always on) and the Free Managed Ruleset (206 blocks on the apex, `securitySource=firewallManaged`); no custom rule, rate limit, Bot Fight Mode or DDoS mitigation had ever fired on any host. **G1 (owner, Dashboard):** the zone-wide Single Redirect `http://* → https://${1}` (301) is retained as intentional HTTPS-enforcement policy, with an apex-specific first-priority carve-out `http://shsl.world/* → https://www.shsl.world/${1}` (301, query preserved) added after the first validation caught apex visitors being sent to the unreachable HTTPS apex; acceptance at 03:39: `ccso` 301 → `https://ccso.shsl.world/` with `/about/` and a multi-parameter query preserved, one hop to 200; `www` 301 → HTTPS 200; apex 301 → `https://www.shsl.world/` 200 with path and query preserved, no loop; production smoke 11/11. **G2 (owner):** Minimum TLS Version raised to 1.2 — TLS 1.0 and 1.1 handshakes now fail (curl exit 35), TLS 1.2 (ECDHE-ECDSA-CHACHA20-POLY1305) and 1.3 (TLS_AES_256_GCM_SHA384) accepted; `ccso` and `www` HTTPS 200; the pre-existing HTTPS apex timeout recorded, not touched. **G3 (owner):** WAF custom rule "ccso-portfolio — block non-read HTTP methods" — `(http.host eq "ccso.shsl.world" and not (http.request.method in {"GET" "HEAD"}) and not starts_with(http.request.uri.path, "/cdn-cgi/"))`, Block — active: POST / PUT / PATCH / DELETE / OPTIONS on `/`, `/about/`, `/robots.txt` and an unknown route answer Cloudflare's 403 block page instead of the previous Static Assets 405, confirmed in zone analytics as `securitySource=firewallCustom`, `securityAction=block`; GET / HEAD unchanged; `/cdn-cgi/rum` — the Web Analytics beacon endpoint that Cloudflare injects into HTML for browser requests — is excluded and not blocked; `www.shsl.world` and `workers.dev` keep their previous behaviour. **G4 (owner):** WAF custom rule "ccso-portfolio — block non-standard HTTP ports" — `(http.host eq "ccso.shsl.world" and not (cf.edge.server_port in {80 443}))`, Block — active: HTTPS on 8443, 2053 and the tested alternates 2083 / 2087 / 2096 blocked (403, `firewallCustom`); port 443 serves production; port 80 still reaches the G1 redirect (Single Redirects run before WAF custom rules, so plain-HTTP alternate ports are upgraded to HTTPS on 443 rather than blocked). **Rule order (Dashboard read-back by the owner after reordering):** 1. `ccso-portfolio — block non-standard HTTP ports`, 2. `ccso-portfolio — block non-read HTTP methods`, both Active / Block — the rule list is not readable from the operator machine (no Zone WAF read scope; firewall-event analytics denied), so the order is recorded from the owner's Dashboard; both actions are Block, so the outcome is order-independent, re-validated at 03:56 HK with dual-match (non-GET on 8443 / 2053), single-match and exclusion probes, all as intended. **Unchanged throughout:** production smoke 11/11 after every control; `workers.dev` healthy (smoke green; a different zone); `PUBLIC_SITE_INDEXABLE` absent → `false`; no rate limiting rule added (reserved for a future state-changing endpoint); Bot Preference Sync OFF (`robots.txt` = build, 165 B); Google Workspace / production DNS baseline identical to the M3B snapshot (apex + `www` A/AAAA, the 5 MX records, SPF, DKIM, DMARC, site-verification, NS; `ccso` A/AAAA). Observed only: one `_acme-challenge.shsl.world` TXT value disappeared during the hardening window — consistent with managed certificate-validation activity, but the available evidence does not establish the exact certificate lifecycle event. Repository, CI, Worker configuration, DNS, variables and secrets untouched by this cycle. State: **M3C-1.5 COMPLETE** · M3 not complete. Next: **Google-Extended content-use decision → M3C-2 public indexability cutover** (separate AUTH).
- **02:34 — M3C-1 custom-domain production endpoint cutover — COMPLETE record landed** — technical acceptance gate met at 02:21 HK on `main` run 36172754060 (the first production deployment whose readiness poll and production smoke ran against `https://ccso.shsl.world`) and 02:23 HK (dual-host read-back); the completion record landed at 02:34 via this pull request. **Cycle:** repository variable `PRODUCTION_URL` switched at 02:11 from `https://ccso-portfolio.secc-biz.workers.dev` to `https://ccso.shsl.world` (read back exactly; every other variable, the secrets, `wrangler.toml`, CI, the Dashboard Custom Domain, bot settings, DNS, Access and the Wrangler version untouched); the start record PR #37 (merge `0041cec`, 02:20 — `verify` + `e2e` SUCCESS, `preview` + `deploy` SKIPPED on the PR, merged CLEAN with a merge commit, no bypass) triggered `main` run 36172754060 (attempt 1, fully green): `verify` SUCCESS (18:20:11–18:20:40Z) → `e2e` SUCCESS (18:20:43–18:21:33Z) → `deploy (production)` SUCCESS (18:21:36–18:21:58Z, started only after both) — step `Deploy` SUCCESS (Wrangler 4.137.0, "No updated asset files to upload", the `workers.dev` URL the only Wrangler-managed trigger, Version `375ec462…`, production deployment 10), step `Production smoke` SUCCESS: readiness `GET https://ccso.shsl.world/` → 200 on attempt 1/24, then `scripts/smoke-preview.sh` 11/11 against `https://ccso.shsl.world` ("all checks passed"); not rerun; deploy log sweep: no emitted error. **Post-deploy dual-host read-back (02:23, anonymous client):** `https://ccso.shsl.world` → 200, valid TLS (Google Trust Services WE1, SAN `ccso.shsl.world`, to 2026-12-24), smoke 11/11, all 24 files of the CI artifact `dist-0041cec…` byte-identical (plus the navigation 404 = `ROUTE_NOT_FOUND`), canonical `https://ccso.shsl.world/`, `noindex, nofollow`, `robots.txt` byte-for-byte = build (165 B); `https://ccso-portfolio.secc-biz.workers.dev` → 200, smoke 11/11, the same 24/24 files, same canonical / meta / `robots.txt` — live as the rollback host only; `PRODUCTION_URL=https://ccso.shsl.world` confirmed, `PUBLIC_SITE_INDEXABLE` still absent (→ `false`), `CLOUDFLARE_PREVIEW_ENABLED` absent; Cloudflare `{enabled:true, previews_enabled:false}` on both APIs, `deployed_on` 18:21:51Z, 10 deployments, 0 Previews; the authoritative DNS / Google Workspace baseline identical to the M3B post-recovery snapshot, `ccso` A/AAAA unchanged, SOA serial 2415840941, DNSSEC off; the Dashboard Custom Domain unchanged (content identity). State: CI production readiness / smoke observe `ccso.shsl.world` · `workers.dev` live only as the rollback host (`workers_dev = true` until M3D) · public indexability OFF · M3 not complete · [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73) still OPEN. Next: the `Google-Extended` crawler-policy decision (owner), then **M3C-2 — public indexability cutover (`PUBLIC_SITE_INDEXABLE=true`; separate AUTH)**, then M3D `workers.dev` retirement.
- **02:20 — M3C-1 started: production acceptance endpoint switched to the custom domain; the merge of this pull request is the first push to `main` with `PRODUCTION_URL=https://ccso.shsl.world`** — preflight on clean `main` `1d8b0bc` (PR #36 merged, local = origin, 0 open pull requests): `https://ccso.shsl.world/` → 200 with valid TLS (Google Trust Services WE1 certificate, SAN `ccso.shsl.world`, valid to 2026-12-24), `scripts/smoke-preview.sh` 11/11, all 24 files of the CI artifact `dist-1d8b0bc…` byte-identical on the host (plus the navigation 404 = `ROUTE_NOT_FOUND`); `https://ccso-portfolio.secc-biz.workers.dev/` → 200, smoke 11/11, the same 24/24 files; canonical `https://ccso.shsl.world/` and `noindex, nofollow` on both hosts; `robots.txt` byte-for-byte identical to the build on both hosts (165 B; Bot Preference Sync still off); the authoritative DNS / Google Workspace baseline (apex + `www` A/AAAA, the 5 MX records, SPF, `google._domainkey` DKIM, `_dmarc`, site-verification TXT, NS) diffed line-for-line against the M3B post-recovery snapshot: identical, `ccso` A/AAAA unchanged, SOA serial 2415840941 unchanged, DNSSEC still off; `wrangler.toml` `workers_dev = true`, `preview_urls = false`; repository variables before this cycle `CLOUDFLARE_DEPLOY_ENABLED=true`, `CLOUDFLARE_WORKERS_SUBDOMAIN=secc-biz`, `PRODUCTION_URL=https://ccso-portfolio.secc-biz.workers.dev`, `PUBLIC_SITE_INDEXABLE` and `CLOUDFLARE_PREVIEW_ENABLED` absent; Cloudflare read-back `{enabled:true, previews_enabled:false}` on both APIs, 9 production deployments (the latest = `1d8b0bc`), 0 Previews; the Dashboard-managed Custom Domain attachment evidenced by content identity (the custom domain serves this Worker's deployed artifact); `pr-27` neither probed nor mutated. **Set at 02:11 and read back exactly: `PRODUCTION_URL=https://ccso.shsl.world`** — the `deploy (production)` job's bounded readiness poll and production smoke now target the custom domain. Untouched: `CLOUDFLARE_DEPLOY_ENABLED`, `PUBLIC_SITE_INDEXABLE` (still absent → every page stays `noindex, nofollow`), `CLOUDFLARE_PREVIEW_ENABLED` (still absent), the secrets, `wrangler.toml` (`workers_dev = true` keeps `workers.dev` live as the rollback host), CI, the Dashboard Custom Domain, bot settings, DNS, Access, the Wrangler version. This pull request changes no code: on the PR `verify` + `e2e` run while `preview` and `deploy` are skipped; the `main` run after the merge is expected to execute `deploy (production)` — `wrangler deploy` of the verified artifact, then readiness and `scripts/smoke-preview.sh` against `https://ccso.shsl.world`. M3C-1 is recorded complete only after that run is fully green and both hosts are re-validated — separate record. Public indexability (M3C-2) is deliberately not part of this cycle.
- **01:38 — M3B Custom Domain attachment + dual-host validation — COMPLETE record landed** — technical acceptance gate met at 01:27 HK on `main` run 36167004068 (recovery deployment fully green) and 01:29 HK (dual-host read-back); the completion record landed at 01:38 via PR #35 (merge `693019a`). **M3A (00:20–00:35, strictly read-only preflight) classified the domain path as Path A**: `shsl.world` is already Cloudflare-authoritative (registry delegation and the zone itself both `apollo` / `cortney.ns.cloudflare.com`; registrar NameCheap), so no nameserver migration is required; DNSSEC off (no DS at the parent, no DNSKEY); `ccso.shsl.world` originally NXDOMAIN (no CNAME / A / AAAA conflict, no route, no certificate); DNS / Google Workspace baseline captured from the authoritative server (apex + `www` proxied A/AAAA, the 5 MX records, SPF, `google._domainkey` DKIM, `_dmarc`, Google site-verification TXT, NS). The owner then attached `ccso.shsl.world` to Worker `ccso-portfolio` through the Cloudflare Dashboard (Cloudflare created the proxied DNS record — zone SOA serial 2415791341 → 2415840941 — and an Advanced Certificate, Google Trust Services WE1, SAN including `ccso.shsl.world`, valid to 2026-12-24). **Initial repo-managed attempt:** PR #33 (merge `3c4890e`, 01:03) declared `[[routes]] pattern = "ccso.shsl.world" custom_domain = true`; the Custom Domain itself was healthy throughout (200, valid TLS, smoke 11/11, content identical to the deployed artifact), but `main` run 36164672742 failed during trigger reconciliation — Cloudflare API code 10084, "The zone \"ccso.shsl.world\" does not exist on your account" — because the deliberately Worker-scoped CI token cannot manage the zone / Custom Domain connection; the script and the `workers.dev` trigger had already deployed (deployment 6) and no service outage occurred on either host. **Least-privilege recovery:** owner decision = do not broaden the CI token, the Custom Domain remains Dashboard-managed; PR #34 (merge `686dd1f`, 01:25) removed the repository `[[routes]]` declaration and recorded the exception in `wrangler.toml`; `main` run 36167004068 fully green — `verify` → `e2e` → `deploy (production)` SUCCESS with the unchanged token (Version `8f4ca423…`, deployment 7, `workers.dev` the only Wrangler-managed trigger), readiness PASS (1/24 → 200), `workers.dev` smoke 11/11. **Dual-host acceptance (01:29, anonymous client):** `https://ccso.shsl.world` → 200, valid TLS, `scripts/smoke-preview.sh` 11/11, 24/24 deployed files byte-identical to the artifact (plus the navigation 404 = `ROUTE_NOT_FOUND`); `https://ccso-portfolio.secc-biz.workers.dev` → the same content, smoke 11/11; canonical remains `https://ccso.shsl.world/` on both; both remain `noindex, nofollow`; DNS / MX / SPF / DKIM / DMARC / Google verification unchanged (authoritative re-read diffed against the pre-attachment snapshot); Preview URLs remain disabled (`{enabled:true, previews_enabled:false}`, `CLOUDFLARE_PREVIEW_ENABLED` absent). The owner disabled Cloudflare Bot Preference Sync (its zone-managed Content Signals block had been prepended to the custom domain's `/robots.txt`); read-back confirmed both the custom domain's and `workers.dev`'s `robots.txt` byte-for-byte identical to the build (165 B). State: dual-host live · custom domain still pre-launch (`PRODUCTION_URL` = `workers.dev`, `PUBLIC_SITE_INDEXABLE` unset → `noindex`) · M2 Preview cleanup issue [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73) still OPEN · M3 not complete. Next: **M3C — custom-domain production endpoint cutover + public indexability** (separate AUTH).

### 2026-09-25

- **23:50 — M2.5 `workers.dev` production deployment — COMPLETE record landed** — technical acceptance gate met at 23:35 HK on `main` run 36154921374; the completion record landed at 23:50 via PR #31 (merge `10cd55e`). Production is live on `https://ccso-portfolio.secc-biz.workers.dev` (temporary host until M3; every page `noindex, nofollow`, canonical `https://ccso.shsl.world/`, `PUBLIC_SITE_INDEXABLE` unset → `false`). Initial production deployment cycle: PR #29 (merge `3c6a6a7`, 22:30) was the first push to `main` with `CLOUDFLARE_DEPLOY_ENABLED=true`; its run 36147964561 deployed successfully (Wrangler 4.137.0, Version `0f0d862c…`, the `workers.dev` URL as the only trigger) while the old inline `Production smoke` step went red twice with the Wrangler deployment green both times — attempt 1 on the very first request, 0.4 s after the route was enabled (no readiness wait); attempt 2 (`gh run rerun --failed`, Version `17ec3c7d…`) deterministically on `curl … | grep -q` under `pipefail` (curl exit 23, confirmed by local reproduction with PIPESTATUS controls). Initial production acceptance at 22:42 from an anonymous client against the verified CI artifact (byte-identical to the local build): 64/64 — `scripts/smoke-preview.sh` 11/11, all 24 `dist/` files byte-identical on the live host, `/about/` placeholder contract (200 + `noindex, nofollow`, excluded from the sitemap), `/case-studies` 307 → `/case-studies/`, navigation 404 = `ROUTE_NOT_FOUND`, robots meta and canonical as above. CI hardening: PR #30 (merge `12de99c`, 23:33; `ci.yml` only) — `deploy (production)` `needs: [verify, e2e]`, a bounded readiness wait (24 × 5 s, per-request timeouts, `::error::` on exhaustion), then the shared `scripts/smoke-preview.sh`. Final accepted `main` run **36154921374** (attempt 1, fully green): `verify` SUCCESS → `e2e` SUCCESS → `deploy (production)` started only after both; Deploy SUCCESS (Version `bd091066…`, production deployment 3); readiness 1/24 → 200; production smoke 11/11; job SUCCESS. Cloudflare read-back: subdomain `{enabled:true, previews_enabled:false}` — the production `workers.dev` route is on while Preview / Version URL routing stays off; production deployments and `deployed_on` populated. `CLOUDFLARE_PREVIEW_ENABLED` remains absent (automated Previews disabled; the Preview cleanup / routing issue [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73) stays OPEN and independent of production acceptance); no DNS or custom-domain work has occurred. Steady state from here: every push to `main` deploys production through `verify` → `e2e` → `deploy` → readiness → production smoke. Next launch milestone: **M3 — `ccso.shsl.world` DNS migration + custom domain** (separate AUTH).
- **22:25 — M2.5 started: production target configured; the merge of this pull request is the first push to `main` with the production deploy enabled** — preflight on clean `main` `b6558ad` (PR #28 merged, local = origin): `wrangler.toml` `workers_dev = true`, `preview_urls = false`, `previews = { }`; repository variables before this cycle: `CLOUDFLARE_WORKERS_SUBDOMAIN=secc-biz` only (`CLOUDFLARE_PREVIEW_ENABLED` absent — automated Previews stay on hold; `CLOUDFLARE_DEPLOY_ENABLED` absent); the four Cloudflare secrets present by name; Cloudflare read-back with the Worker-scoped token: Worker `ccso-portfolio` 0 deployments, 0 versions, 0 Previews, `deployed_on: null`, subdomain `{enabled:false, previews_enabled:false}`, production `https://ccso-portfolio.secc-biz.workers.dev/` → 404 `error code: 1042` (its `/robots.txt` still Cloudflare's platform "content signals" text — no Worker serves the host yet); Wrangler 4.137.0 unchanged; the old `pr-27` alias neither probed nor mutated. Local gates on `b6558ad`: `astro check` 0 errors / 0 warnings, `astro build` 15 pages, `verify-dist` 547/547, `test:tools` 25/25, Playwright 20 passed / 2 skipped. Set at 22:25 and read back (`gh variable list` + per-variable GET): `PRODUCTION_URL=https://ccso-portfolio.secc-biz.workers.dev` (the `deploy` job's production smoke target) and `CLOUDFLARE_DEPLOY_ENABLED=true`. Untouched: `CLOUDFLARE_PREVIEW_ENABLED` (still absent), Access policies, DNS, custom domains, `PUBLIC_SITE_INDEXABLE` (unset → CI default `false`: every page stays `noindex, nofollow`, canonical stays `https://ccso.shsl.world/`). This pull request changes no code: on the PR `verify` + `e2e` run while `preview` and `deploy` are skipped; the `main` run after the merge is expected to execute `deploy (production)` — `wrangler deploy` of the artifact that passed `verify` (expected to enable the production `workers.dev` route through `workers_dev = true` while enforcing `preview_urls = false`), then the production smoke against `PRODUCTION_URL`. M2.5 is recorded complete only after the production acceptance checks pass — separate record.
- **21:31 — M2 cleanup / routing provider issue reported upstream; durable `preview_urls = false` hardening lands with this pull request** — [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73) filed with the non-secret evidence only (Static Assets Worker, Wrangler 4.137.0, Preview `pr-27`: automated create / update succeeded, `wrangler preview delete --name pr-27 --skip-confirmation` succeeded, cleanup run succeeded, Preview list empty, GET by id → 10025, owner disabled Preview URLs, API `enabled=false` / `previews_enabled=false`, alias still 302 anonymous / 200 authenticated 107 min after the toggle and 4 h 13 min after the deletion, never-requested route and navigation 404 prove live routing, production undeployed throughout, controls inactive within ~30–55 s; documentation mismatch quoted — `preview delete` "Delete a Preview and all its deployments", "Disabling Version URLs disables routing to Version URLs and aliased Version URLs"; asking expected lifecycle / delayed propagation / routing bug). This pull request makes the hold durable in the repository: `wrangler.toml` `preview_urls = false` (explicit — the file said `true`, which the next `wrangler deploy` would have pushed back to the account over the dashboard "off"; Cloudflare documents that an omitted key leaves the existing setting untouched but states no intent), `workers_dev = true` unchanged for M2.5, the empty `previews = { }` block kept (the required Preview configuration namespace, independent of the routing switch); `docs/DECISIONS.md` gained "2026-09-25 — M2 Preview hold". Untouched: `CLOUDFLARE_DEPLOY_ENABLED` (absent), the GitHub secrets, `CLOUDFLARE_WORKERS_SUBDOMAIN`, Access policies, DNS, custom domains; no Preview or production deployment in this cycle (local gates only: `astro check`, build, `verify-dist`, `test:tools`, Playwright against `wrangler dev`, which parses the new config). State: M2 core automation validated · M2 cleanup / routing provider issue OPEN · automated Preview URLs disabled · M2 not complete · launch path unblocked → next operational milestone M2.5 `workers.dev` production deployment (separate AUTH).
- **17:06 — M2 ON HOLD: the Preview cleanup acceptance gate failed; automated Previews disabled** — PR #27 merged 14:39 (`3f58db8`; `verify` + `e2e` + `preview (pr-27)` green on both runs, `deploy` skipped) and `preview-cleanup` run 36103863659 succeeded at 14:40 (`wrangler preview delete --name "pr-27" --skip-confirmation` → "✨ Preview "pr-27" deleted successfully."; Cloudflare Preview list empty; `GET …/previews/76618c57…` → 10025 "Preview not found") — control-plane deletion OK. Yet the named alias `https://pr-27-ccso-portfolio.secc-biz.workers.dev` kept routing: anonymous → 302 to the Access login, service token → 200 with the full site, sampled every 20 s for 35 min, again after a 6-minute quiet window (15:21), on the never-requested `/journey/` (15:22) and with the Worker's own `ROUTE_NOT_FOUND` page on an unknown navigation route ⇒ live routing, not a cache; the two Phase B aliases deleted the same way had gone inactive within ~30–55 s (still 404 at 14:53). Owner decision ("AUTH: hold M2 and disable automated Previews"): the cleanup gate FAILED; repository variable `CLOUDFLARE_PREVIEW_ENABLED` deleted (read back: not found; `CLOUDFLARE_WORKERS_SUBDOMAIN=secc-biz` unchanged; the four secrets present by name; `CLOUDFLARE_DEPLOY_ENABLED` absent) → `preview` job and `preview-cleanup.yml` dormant; the owner also disabled the Worker's Version / Preview URLs in the dashboard (17:06:37, Worker `updated_on`). Final read-only probe 18:53:47 (107 min after the toggle, 4 h 13 min after the deletion): API `{enabled:false, previews_enabled:false}`; alias anonymous → 302, with the service token → 200 (29 926 B homepage, `x-robots-tag: noindex`); production `workers.dev` 404 `error code: 1042`, 0 deployments, `deployed_on: null`; no polling afterwards. M2 is NOT complete: the core automation is validated, the cleanup / routing behaviour is a provider issue to report upstream (evidence preserved outside the repository: workflow logs, API read-backs, probe outputs).
- **14:20 — M2 final cycle started: GitHub Actions secrets and variables configured; automated pull-request Preview validation being exercised by this PR** — repository secrets `CLOUDFLARE_API_TOKEN` (the Worker-scoped Editor token validated in Phase B), `CLOUDFLARE_ACCOUNT_ID`, `CF_ACCESS_CLIENT_ID`, `CF_ACCESS_CLIENT_SECRET` (set with `gh secret set --repo`, values fed from the owner's mode-600 local file through stdin, never echoed; names verified with `gh secret list`) and repository variables `CLOUDFLARE_WORKERS_SUBDOMAIN=secc-biz`, `CLOUDFLARE_PREVIEW_ENABLED=true`; `CLOUDFLARE_DEPLOY_ENABLED` deliberately absent (production deploy stays off until M2.5) and `PUBLIC_SITE_INDEXABLE` unset (CI default `false`, pre-launch). **M2 is not complete**: this pull request is the first automated Preview (`pr-<number>`); its Preview job, Preview smoke, comment and the post-merge cleanup are observed before M2 is recorded complete. **Observed 14:30–14:35 on PR #27, head `14d400e` (CI run 36103016463):** required `verify` and `e2e` SUCCESS; `preview (pr-27)` SUCCESS — create + exact-hostname pin, then `Preview smoke` 11/11 against `https://pr-27-ccso-portfolio.secc-biz.workers.dev` with the service token (06:29:50–51Z), then the pull-request comment (06:29:52Z, `github-actions[bot]`, "built from 14d400e…") — the URL was posted only after the smoke passed; `deploy (production)` SKIPPED. Independently from a local client: anonymous `/` (bare and navigation) and `/robots.txt` → 302 to the Access login; with the Access service token → 200, `scripts/smoke-preview.sh` 11/11, `/`, `/robots.txt`, `/sitemap-index.xml`, `/about/` byte-identical to the local `dist/`, `x-robots-tag: noindex`; Cloudflare Preview list = `pr-27` only; production `workers.dev` 404 `error code: 1042`, 0 deployments, `deployed_on: null`; `CLOUDFLARE_DEPLOY_ENABLED` absent. This evidence commit updates the same `pr-27` Preview in place.
- **13:30 — Homepage simplified: the site ends on evidence, not a conversion funnel** — PR #25 merged with a merge commit (`121c1df`; `verify` + `e2e` green on the PR run 36098691386 and again on `main` run 36098839836, no bypass; `preview` / `deploy` skipped — no Cloudflare mutation in this cycle). Owner design decision: the homepage reads as a senior professional's curated evidence portfolio, not a consulting / lead-generation landing page. Removed from `src/pages/index.astro`: the About teaser card (`identity.thesis` + "More about me") and the "Explore the case studies, or get in touch." CTA section, with their scoped CSS — the page now ends on the Lab / Writing teaser (Hero, capability strip, Featured case studies, Journey, Capability → Evidence, Working principles unchanged). Removed from the shared `src/components/SiteFooter.astro`: the "Have a problem worth solving well?" contact card (it was every page's ending, so the removal is site-wide; a homepage-only layout prop was rejected); the footer identity block is now one line (`identity.name — identity.role`) + the alias note + lightweight links built from `src/data/site.ts`, rendering only links with a real destination (GitHub / Feuer · Email today; LinkedIn appears once `contact.linkedin` is set — owner review adjustment applied before push: no "Soon" placeholder in the shared footer); footer navigation and the legal line unchanged; the footer no longer carries a heading. `docs/DECISIONS.md` gained "2026-09-25 — Homepage ending: evidence, not a conversion funnel". Evidence on the PR head `b7a8e6f`: `astro check` 0 errors / 0 warnings, `astro build` 15 pages, `verify-dist` 547/547, `test:tools` 25/25, Playwright 20 passed / 2 skipped; rendered against `wrangler dev`: homepage 5608 → 4586 px (desktop 1280) and 8102 → 6901 px (mobile 412), 9 → 7 `main` sections, footer 583 → 317 px, headings h1 → h2 → h3 with no level skips, one `mailto` and one GitHub link left (footer), About / Contact reachable from header + footer navigation, section rhythm before the footer unchanged. Read back from `main` by an unauthenticated client after the merge: `src/pages/index.astro`, `src/components/SiteFooter.astro`, `docs/DECISIONS.md` byte-identical to the PR head.
- **12:43–12:46 — M2 Phase B: Worker-scoped Editor API token validated (create / update / delete)** — every Wrangler command ran with `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` from the owner's local file under an isolated Wrangler config directory (control arm without the token → "not authenticated", so no fallback to the owner's OAuth login was possible; `wrangler whoami` → "Account API Token … read from the CLOUDFLARE_API_TOKEN environment variable"). `npx wrangler preview --name m2-token-smoke --json` → Preview `b900835b…`, Deployment 1 `70611a92…`; `scripts/extract-wrangler-preview-url.mjs --expect-hostname m2-token-smoke-ccso-portfolio.secc-biz.workers.dev` accepted the URL; anonymous → 302 to the Access login on the named and the immutable Deployment URL (a bogus service token gets the same 302); with the Access service token → `scripts/smoke-preview.sh` 11/11 and `/`, `/robots.txt`, `/sitemap-index.xml`, `/about/` byte-identical to `dist/` on both hosts, canonical `https://ccso.shsl.world/`, robots `noindex, nofollow`, platform `x-robots-tag: noindex`. A second run of the same command updated the Preview in place (same Preview id, Deployment 2 `fe6b8715…`, message annotation recorded). `wrangler preview delete --name … --skip-confirmation` removed `m2-token-smoke` and then `m2-access-smoke` (one at a time; API list empty, `GET …/previews/<id>` → 10025 "Preview not found"). After deletion: both named hosts → anonymous 302, service token 404 (inactive); the immutable Deployment URLs `70611a92`, `fe6b8715`, `e64693b4` and the 00:42 smoke's `fe534f76` (its Preview deleted ~12 h earlier) → anonymous 302, service token 200 with the full site, unchanged 17 minutes later — a live deployment, not a cache (`/nope-<ts>/` returns the Worker's own 9-byte 404 body; `cf-cache-status: HIT` is a Static Assets platform header on every response). Production `workers.dev` still 404 `error code: 1042`, 0 deployments, subdomain `{enabled:false, previews_enabled:true}`. Owner verdict: PASS — local least-privilege Preview capability validated; `preview delete` ≠ Deployment URL revocation stands, with Access as the control.
- **12:27 — M2 Phase A: Cloudflare Access "Previews only" validated on the owner-OAuth Preview `m2-access-smoke`** — account side (owner, Cloudflare dashboard, ~10:35–12:02): Zero Trust team `s-e-c-c`; Access application on `*-ccso-portfolio.secc-biz.workers.dev` with a human (e-mail) policy and a Service Auth policy for a service token; Worker `ccso-portfolio` subdomain flags `enabled: false` (production `workers.dev` off) and `previews_enabled: true`; the service token and the Worker-scoped Editor API token kept in a mode-600 local file outside the repository (`CF_ACCESS_CLIENT_ID` / `CF_ACCESS_CLIENT_SECRET` / `CLOUDFLARE_API_TOKEN`; the Client Secret follows Cloudflare's new `cfast_…` format). Observed: every `*-ccso-portfolio.secc-biz.workers.dev` host — named, Deployment, deleted, even nonexistent names — answers 302 → `https://s-e-c-c.cloudflareaccess.com/cdn-cgi/access/login/<host>` to anonymous requests (Access matches the wildcard before any Worker lookup), while production `ccso-portfolio.secc-biz.workers.dev` stays 404 `error code: 1042`; with the service token the named `m2-access-smoke-…` and the immutable `e64693b4-…` Deployment URL both pass `scripts/smoke-preview.sh` 11/11 with `/`, `/robots.txt`, `/sitemap-index.xml`, `/about/` byte-identical to `dist/` (a first attempt at 12:05 was rejected until the owner fixed the Access side — the credential file was unchanged). The owner's browser login through Access passed afterwards (human policy). Also learned: on a host no Worker serves, `/robots.txt` returns Cloudflare's platform "content signals" text (1248 B), a platform artifact, not a deployment; the Worker's own 165-byte `robots.txt` wins wherever the Worker serves.
- **01:55 — M2 automation hardening landed** — PR #23 merged with a merge commit (`a44267e`; `verify` + `e2e` green on the PR run 36037305616 and again on `main` run 36037674763, no bypass; `preview` / `deploy` skipped — both gates unset, no Cloudflare mutation in this cycle). Landed: two independent gates — `CLOUDFLARE_PREVIEW_ENABLED` (pull-request Previews + `preview-cleanup.yml`) and `CLOUDFLARE_DEPLOY_ENABLED` (production deploy on `main`) — so enabling PR Previews can no longer production-deploy the next merge; `scripts/extract-wrangler-preview-url.mjs` (fail-closed Preview URL extraction from Wrangler's mixed stdout; `--expect-hostname` accepts only exactly `pr-<n>-ccso-portfolio.<CLOUDFLARE_WORKERS_SUBDOMAIN>.workers.dev` before any credential follows the URL) with the real controlled-Preview output as the regression fixture (author fields redacted, subdomain replaced by `example-subdomain`); `scripts/smoke-preview.sh` (curl: 200s, real 404 for any unknown route, the `ROUTE_NOT_FOUND` page on a browser navigation, 404 page `noindex` without canonical, `robots.txt` / `sitemap-index.xml`, trailing-slash redirect, canonical, robots meta per `PUBLIC_SITE_INDEXABLE`; optional Cloudflare Access service token `CF_ACCESS_CLIENT_ID` + `CF_ACCESS_CLIENT_SECRET`, both or neither — exactly one refuses before any request); preview job order create → extract + pin → smoke → comment; `npm run test:tools` (25 tests, including a loopback mock that records every request the smoke makes) in the required `verify` job after `test:site` — same check context, no ruleset change; `docs/DECISIONS.md` section "2026-09-25 — M2 automation hardening". Owner review adjustments applied before push: subdomain redacted from the fixture, `test:tools` moved into `verify` (not repeated in the preview job), smoke before the comment, Access service-token support, exact-hostname pin. Read back from `main` by an unauthenticated client: all nine files byte-identical to the merge; `verify` on `main` ran `test:tools` (step 8, success).
- **00:42–00:57 — M2 controlled Worker Preview (first authenticated Cloudflare run; owner OAuth login, local only)** — on clean `main` `36a53cf` (Wrangler 4.137.0; `npm ci` / `build` / `test:site` exit 0): `npx wrangler preview --name m2-smoke --json` succeeded on the assets-only project **without a script** — Wrangler synthesised `no-op-worker.js` as the `main_module`, uploaded 24 assets and created the Worker `ccso-portfolio` (before: `deployments list` → "This Worker does not exist" 10007, identical to a nonexistent-name control; after: `[]` — no production deployment or version; the production `workers.dev` host answers 404 `error code: 1042`). Read back unauthenticated on the Preview URL (`m2-smoke-ccso-portfolio.<subdomain>.workers.dev`) and on the immutable Deployment URL: `/` 200 and byte-identical to `dist/index.html`; `robots.txt` / `sitemap-index.xml` 200 and byte-identical, no `Sitemap:` line; `/case-studies` 307 → `/case-studies/`; canonical `https://ccso.shsl.world/`; every live, placeholder and maintenance route `noindex, nofollow`; unknown routes 404. Three findings: (1) the edge serves `dist/404.html` only to navigation requests (`Sec-Fetch-Mode: navigate`; a bare curl gets a 9-byte `Not found`, still 404) while `wrangler dev` serves it to every client — confirmed with the header as the only variable and by the compatibility flag `assets_navigation_prefers_asset_serving`; (2) `wrangler preview --json` prints the asset-upload progress to stdout before the JSON, so the `jq` parser in `ci.yml` failed on the captured output (the key in use is `.preview.urls[0]`); (3) every Preview response carries a platform `x-robots-tag: noindex`. Deleted with `wrangler preview delete --name m2-smoke --skip-confirmation` (exit 0; the Preview URL then answers Cloudflare's 404 page; the Worker remains) — the immutable Deployment URL was still serving the site an hour later. Owner decisions (01:10): core PASS, M2 not yet complete; no architecture change for the 404; `preview delete` ≠ Deployment URL revocation, so Access "Previews only" precedes automation; `X-Robots-Tag` is evidence, not contract; the parser is a fail-closed helper, not `sed`.

### 2026-09-24

- **19:56 — `e2e` promoted to a required check; CI governance closed** — `protect-main` updated through the API (the ruleset update endpoint): `required_status_checks` = `verify` + `e2e`, both from the GitHub Actions app, `strict: false` unchanged; every other field unchanged, confirmed by a structural diff of the full read-back against the pre-change state (the only difference is the added `e2e` entry); GitHub's branch-rules evaluation for `main` lists both. Threshold met first: three consecutive normal pull requests with `e2e` green (#19, #20, #21). Final governance: pull request required → `verify` + `e2e` required → merge commits only → 0 approving reviews, review-thread resolution, deletion / force-push blocked, Repository Admin bypass PR-only. Validated on this record's own pull request (#22) without issuing a merge command while blocked, sampled every 3 seconds: `BLOCKED` one second after the PR opened (19:58:43, no check reported yet); `BLOCKED` with `verify` in progress (19:58:53); **still `BLOCKED` with `verify` SUCCESS while `e2e` was queued (19:59:12) and then in progress (19:59:16)** — the newly required `e2e` gate holds on its own; `CLEAN` only once `verify` and `e2e` had both passed (20:00:07). `preview` / `deploy` skipped throughout.
- **19:50 — Artifact Actions Node 24 maintenance COMPLETE** — PR #21 merged with a merge commit (`dd4b3c1`; `verify` + `e2e` green, no bypass): `actions/upload-artifact` v5.0.0 (`330a01c`, node20) → v7.0.1 (`043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`, node24) in `verify` and the failure-only Playwright report step; `actions/download-artifact` v5.0.0 (`634f93c`, node20) → v8.0.1 (`3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c`, node24) in `e2e`, `preview` and `deploy`; `actions/checkout` v5.1.0 and `actions/setup-node` v5.0.0 already declare node24 and were left alone; exactly five pin lines changed, no workflow logic. Release notes read for both: upload v7's new `archive` input defaults to `true` (zipped upload unchanged); download v8 defaults `digest-mismatch` to `error` — accepted as fail-closed hardening (a corrupted `dist` must not reach `e2e`, Preview or deploy). Read back: the two Node 20 runtime warnings of the previous `main` run are gone (0 on the PR run and on `main` run 35995392566); `verify` uploaded `dist-<sha>` (24 files) and `e2e` downloaded it with a verified digest; Playwright 20 passed / 2 skipped; `preview` / `deploy` skipped. `e2e`-green pull requests: 3/3 (#19, #20, #21) — promotion threshold reached.
- **19:07 — Portfolio `main` ruleset applied** — `protect-main` (ruleset 23934021) created through the API after M1's `verify` had run green on `main`: pull request required (0 approving reviews, review-thread resolution), required status check `verify` (`strict: false`), deletion and force-push blocked, merge commits only, bypass = Repository Admin role in pull-request mode only. Read back in full and diffed against the intended payload: identical apart from two GitHub-supplied defaults (`required_reviewers: []`; `require_extra_approval_for_unattributed_changes: true` — the same default present on the five solo-maintained repository rulesets, which merged 0-review PRs earlier today). Validated on this record's own pull request (#20) without issuing a merge command while blocked: `mergeStateStatus` `BLOCKED` one second after the PR opened (19:11:32, `mergeable: MERGEABLE`, no check reported yet, no review requested), still `BLOCKED` at 19:12:02 with the required `verify` in progress, `CLEAN` at 19:13:49 once `verify` and `e2e` had passed (`preview` / `deploy` skipped). `e2e` green on #20 → `e2e`-green pull requests toward promotion: 2/3 (#19, #20).
- **18:56 — M1 COMPLETE (technically deployable)** — PR #19 merged with a merge commit (`4258718`; `verify` green, `e2e` green, no bypass; `preview` / `deploy` skipped — dormant behind the repository variable `CLOUDFLARE_DEPLOY_ENABLED`); `verify` + `e2e` green again on `main` (run 35990123473). Landed: Node ≥ 22.12 (`engines`, `.nvmrc`); Wrangler 4 and the `wrangler.toml` production contract (`not_found_handling = "404-page"`, `html_handling = "auto-trailing-slash"`, `workers_dev`, `preview_urls`, empty `previews` block for Worker Previews); Astro `trailingSlash: 'always'`; the route-state policy `src/data/routes.mjs` (single source for placeholder / maintenance / 404 states, shared by the sitemap filter, `BaseLayout`, `robots.txt` and the dist gate); typed `PageMeta` in `BaseLayout` with the policy as the robots floor, titles `<Page> | Eric So`, no canonical on 404, `apple-touch-icon`; `PlaceholderPage` (In Preparation: 200 + `noindex, nofollow`) for `/about/`, `/internal-builds/`, `/writing/`, `/topics/`; custom 404 (`ROUTE_NOT_FOUND`, real 404 status, `noindex`); `/under-maintenance/` chromeless + `noindex, nofollow`; `robots.txt` endpoint (AI-crawler policy from `docs/DECISIONS.md`; `Sitemap:` only when `PUBLIC_SITE_INDEXABLE=true`); sitemap always built, live routes only; `scripts/verify-dist.mjs` (zero dependencies, 547 checks in both modes); Playwright smoke (desktop + Pixel 7) against `wrangler dev`; `ci.yml` (`verify` → `e2e` → dormant `preview` / `deploy`, actions pinned to commit SHAs) and `preview-cleanup.yml`. Owner review adjustments applied before push: empty `previews = { }`; no production deploy assumed before the first Worker Preview; `e2e` a normal, red-capable job that the ruleset does not require yet; Preview URL parsed with `jq -er '.preview_urls[0] // .preview.urls[0]'`. Read back from the `main` CI artifact: 404 `noindex` without canonical; placeholders and maintenance `noindex, nofollow`; every page `noindex, nofollow` pre-launch; sitemap = exactly the 9 live routes; `robots.txt` = the five policy blocks, no `Sitemap:` line. The superseded local branch `docs/deploy-readiness-proposal` was discarded (the owner's roadmap and M1 replaced it).
- **~17:51 — StarLedger Template sync COMPLETE; the StarLedger / Template cycle is closed** — starledger-template PR #5 merged with a merge commit (`ff93cbe`): regenerated from `starledger@e4ec5b7` with `pnpm template:build --force --verify` (install / typecheck / test / build / `setup:doctor --template-clean` all green); generated output vs the template's `main`: added 0 · removed 0 · changed 1 (`README.md`, the verbatim `README.template.md`) · unchanged 366; the template's `verify`, `verify-agent-artifacts` and `verify-ai-provenance` checks green on the PR. Read back: the template's `README.md` on `main` is byte-identical to the parent's `README.template.md@e4ec5b7` — H1 `StarLedger Template`, template value line, ACTIVE + Apache-2.0, no child-release version badge (D18).
- **~17:34 — StarLedger Step 5 parent cycle COMPLETE** — starledger PR #290 merged with a merge commit (`e4ec5b7`; required `verify`, `verify-agent-artifacts`, `verify-ai-provenance` and the triggered template-smoke `build-and-check` all green; no bypass). `README.md`: bold value proposition (the repository description), status · static exact-version `v1.3.0-alpha.1` badge linked to that Release · Apache-2.0 badge, "Live dashboard →" link, a three-sentence business-readable summary, five evidence-backed highlights, then Quick start; the phase table and contracts line moved unchanged into `Phases and contracts` immediately below Quick start; nothing version- or identifier-shaped above Quick start except the badge. `README.template.md`: H1 `StarLedger Template`, the template repository's description as value line, Apache-2.0 badge beside the status badge — deliberately no child-release version badge (a parent-maintained file must not carry the template repository's release state). No release for this cycle. Read back from `main` by an unauthenticated client: all eleven confirmation points pass. The template repository is regenerated from `e4ec5b7` in the next, separate cycle.
- **~17:10 — Worldview Council Step 5 cycle COMPLETE** — religion-council PR #64 merged with a merge commit (`e50b51a`; required `test (3.9)` / `test (3.11)` green, branch up to date, no bypass). README top fold made business-readable: the v0.5 → v0.13.1 narrative replaced by a version-independent current-state summary (three execution modes; the opt-in strict path checks structured claims against curated sources and refuses unverifiable quotations — fails closed; one versioned, conformance-tested retrieval contract; default retriever lightweight and file-based, no vector or network RAG backend adopted) plus a `CHANGELOG.md` link; `Why it's different` condensed from seven to five evidence-backed bullets, the forward-looking "Built for a RAG future" bullet replaced by "One retrieval contract, multiple implementations"; the strict-finalization subsections moved below Quick start under `Strict mode (opt-in)` / `嚴格模式 (opt-in)`; implementation detail (identifiers, thresholds, ADR references) moved below the fold in the project's existing wording. EN and 繁體中文 halves mirror. No release (D4). Read back from `main` by an unauthenticated client: five bullets in both halves, Quick start above the detail sections, no version narrative / identifiers / thresholds / ADR references above Quick start, CHANGELOG link resolves; push CI green on `e50b51a`.
- **~16:30 — Step 5 decisions recorded (D11–D17); standard and issue #1 aligned** — the owner's decisions on the Step 5 proposal recorded as D11–D17 in the review record (AI Arena: MIT code + contribution/content policy with the revised rights / removal / confidentiality wording, `v0.1.0` as a normal release, `zh-Hant` filenames; Worldview Council: version-independent current-state summary only; StarLedger: static `v1.3.0-alpha.1` badge; homebrew-tap: BSD-2-Clause with the app's AGPL-3.0 stated; portfolio: code MIT + original content All Rights Reserved via `NOTICE.md`; bilingual filenames `zh-Hant` with the 繁體中文（香港） label kept; forks reworded + archived — a public fork cannot be made private in place); standard §3 / §5 / §9 / §10 updated; issue #1 Step 5 rewritten in execution order. Facts verified first: GitHub's fork-visibility rule; shields' dynamic release badge renders "no releases" for StarLedger. Owner adjustments before execution: `LICENSE` holder Eric So; two PR-template checkboxes; D11–D17 kept out of the historical review sections, added as a "Post-review implementation decisions" addendum.
- **~16:25 — AI Arena Step 5 cycle COMPLETE — `v0.1.0`** — PR #28 squash-merged at 16:20 (`621975f`; required `validate` green, no bypass): MIT `LICENSE` (holder Eric So), contribution/content policy in `CONTRIBUTING.md` + `CONTRIBUTING.zh-Hant.md` with two PR-template confirmation checkboxes, `README.zh-TW.md` / `CONTRIBUTING.zh-TW.md` → `zh-Hant` (every reference updated), README top fold EN + zh-Hant (value line, status / version / license badges, live gallery link, highlights condensed from existing evidence, `Daily Workflow` kept as quick start) and a License section scoping MIT to the code. Annotated tag `v0.1.0` on the squash commit; normal GitHub Release published (first tag of the repository). Read back: tag object → `621975f`; Release not a pre-release and reported as latest; GitHub licence detection `mit`; `main` CI validate + deploy green on `621975f`; live site 200; new files 200 and old `zh-TW` paths 404 from an unauthenticated client.
- **~05:15 — Step 4 housekeeping** — the one known residue of the template-generation contract closed at the source: starledger #288 merged (`b12b94e`) — `deploy-freshness.yml` joins `NEUTRALIZE_SCHEDULE_WORKFLOWS` (emitted dispatch-only; on a fresh template it could only alarm, daily, with no committed dataset and no Pages site yet), and the builder now fails if any emitted workflow still carries a live `schedule:` trigger (6 new tests; parent CI incl. the template self-test green). starledger-template #4 merged (`094b271`): regenerated from `starledger@b12b94e` with `--verify` green — 10 changed paths, 0 added / removed, all inside the builder allowlist; read back from `main`, none of the 9 shipped workflows fires on its own. Also corrected here: the Step 4 COMPLETE entry's time (~04:30, not 05:30 — PR #14 merged 20:29Z) and the Output summary line (steps 2–4 complete).
- **~04:30 — Step 4 COMPLETE** — (A) starledger #287 merged: the template builder never emits parent-only workflows (`EXCLUDE_WORKFLOWS`) and omits `# template-builder: omit` steps from `ci.yml` (fail-closed transform, 8 new tests; parent CI incl. the template self-test green). (B) starledger-template #3 merged (`7cf81f5`): regenerated from `starledger@44065b5` with `--verify` green, two commits (content sync; `.github` alignment), its CI green for the first time since generation; #1/#2 closed as superseded. (C) ai-pet-usage: `swift-tests` runs on every PR (#104), stable context name (#105), sole required status check (`strict: false`), validated by #106 (blocked while pending, merged without approval once green). Eight Tier-A lifecycle badges read back from `main`: 8/8.
- **~03:55 — Step 4b 7/8; rulesets aligned** — the five solo-maintained repos' rulesets set to PR + required CI, 0 approving reviews (`ai-arena` also `require_last_push_approval` off); every other rule and the bypass list untouched, verified by full ruleset read-back diffs. Badge PRs then merged without bypass: ai-pet-usage #103, skills #248, religion-council #63 (merge), tailscale-ai-egress #35, ai-arena #27 (squash — linear history). starledger-template: the formatting PR #2 exposed that its CI is structurally broken (inherited workflows reference `scripts/smoke-notifier-*.mjs` and `README.template.md`, which the sanitized template does not contain) — both PRs held pending a decision.
- **~03:15 — Step 4a COMPLETE; 4b partial** — descriptions + topics applied on all 9 public repos via `gh repo edit` (sequential; before/after captured; API read-back: descriptions exact, topics set-equal; visibility/homepage/default branch/license untouched). Status-badge PRs opened on all 8 Tier-A repos: merged portfolio #11 (BUILDING) and starledger #286 (ACTIVE); blocked by review rulesets with CI green — ai-pet-usage #103, skills #248, tailscale-ai-egress #35, religion-council #63, ai-arena #27; starledger-template #1 blocked by a pre-existing `prettier --check` failure in two issue-template YAML files (the `CI` workflow has failed on `main` since 2026-06-24).
- **~02:40 — Step 3 COMPLETE** — `skills` badge/`checks.py` PR #247 merged by owner bypass (explicit exception recorded on the PR; required CI checks green on the PR and again on the merge commit `df1248e`); annotated tag `v0.1.16` on the merge commit; GitHub pre-release published with notes covering the evolution since `alpha-0.1.2` without inventing intermediate tags.
- **~02:30 — Step 3 B/C done** — StarLedger `v1.3.0-alpha.1` pre-release cut (Discovery Inbox, P5) and the `v1.2.0-alpha.1` title clarified ("Reusable template (P4) alpha", body unchanged); StarLedger Template `v1.2.0-alpha.1` pre-release cut; `skills` badge/`checks.py` PR #247 opened (CI green; awaiting the ruleset's required review before tag `v0.1.16` + pre-release).
- **~02:10 — `tradingview-mcp` made private** and removed from public-repository / portfolio scope: Lab entry deleted, standard's derivative tier removed as empty (forks are now Tier B), issue #1 items removed; the review record keeps its historical findings with a post-review note.
- **~02:00 — Step 2 COMPLETE** — portfolio PR #6 (`6cee668`: README wording, ROADMAP state, Opus Pack journey chronology with evidence pointers, AI Pet Usage in Lab, `opus-pack` → `skills` rename record, deploy-readiness decisions) and the `tradingview-mcp` README truth-fix PR #28 (`41ff043`) merged.
- **~01:20 — Issue #1 re-based** — body replaced by a six-step implementation checklist that references the standard by section; the original 2026-09-23 body is preserved in the review record's Appendix A.
- **01:09 — PR #5 merged** (`cc9cb86`) — [`docs/PUBLIC_REPO_STANDARD.md`](docs/PUBLIC_REPO_STANDARD.md) v1 (tiers, description, top fold, topics, releases with the `ai-pet-usage` governed exception, five-state status, integrity, licensing state) + [`docs/reviews/2026-09-24-public-repo-metadata-review.md`](docs/reviews/2026-09-24-public-repo-metadata-review.md) + DECISIONS entries.
- **~00:00–00:50 — Public-repo metadata review** — three independent Sonnet 5 read-only gates (metadata audit, integrity critic, release-tooling trace); every material finding reproduced first-hand and GitHub state verified unchanged afterwards. Found: issue #1 had drifted from the consolidated standard; the `ai-pet-usage` tag scheme is a runtime/distribution contract (updater, release workflow, Homebrew cask); the Opus Pack journey row was the review's illustrative example verbatim.

### 2026-09-23

- **23:48 — PR #4 merged** (`096a4e9`) — Grok 4.6 fixes ×2 + ROADMAP/README; PR #2 closed as merged in the same merge (its branch was the base of `review/grok-4.6`).
- **23:45 — Grok 4.6 round-2 fixes committed** (`e4de03a`) — header-wrap regression fixed (desktop header trimmed to the 5 primary sections; Soon routes in footer + mobile menu), evidence honesty (only reachable evidence shown; a placeholder note when none), footer Soon badges, boot a11y (page inert while the overlay is up), hero alias footnote dropped.
- **~23:30 — Grok 4.6 round 2** (`xcheck --grok`) — validated round 1 and caught a regression (the longer brand re-opened the nav wrap) plus incomplete round-1 fixes.
- **23:15 — Grok 4.6 round-1 fixes committed** (`8eee0d9`) — Topics marked Soon, boot FOUC fixed (localStorage skip before paint), Lab-teaser WCAG + StatusBadge, evidence de-duplication, brand positioning, mobile build-stream dots + pet/footer clearance.
- **~22:50 — Grok 4.6 round 1** (`xcheck --grok`) — product / IA / content / design review; 7 findings (no high-severity).
- **21:15 — Roadmap published.** This file added and merged to `main`.
- **20:43 — PR #2 opened** — [Phase 1 cross-model review fixes](https://github.com/F-e-u-e-r/portfolio/pull/2) (`review/phase1-cross-model` → `main`); merged 23:48 together with PR #4.
- **20:42 — Review record committed** (`fbaf940`) — `docs/reviews/2026-09-23-phase1-cross-model.md` (lands with PR #2).
- **20:36 — Review fixes committed** (`a0934c3`) — 11 confirmed xcheck findings fixed; `astro build` (15 pages) + `astro check` (0 errors) green; header re-rendered at 960px; OG image 1200×630 generated.
- **20:00–20:35 — xcheck (cross-model review)** of Phase 1 by `gpt-5.6-sol` + `gpt-5.6-luna` (max effort). Round 1 complete (11 findings, no high-severity); each reproduced before fixing. Round 2 re-review quota-blocked (see xcheck output).
- **19:56 — Issue #1 opened** — [public-repo README / metadata cleanup](https://github.com/F-e-u-e-r/portfolio/issues/1): 9 public repos benchmarked against the `ai-pet-usage` gold standard.
- **19:54 — Phase 1 committed + pushed to `main`** (`386efbd`) — foundation + homepage shell.
- **~19:42 — Phase 1 build first green** — `astro build` 15 pages, `astro check` 0 errors.
- **17:51–19:40 — Phase 1 built** — Astro 7 static scaffold, design system, typed content schema, full IA routing, Matrix boot, Journey + Applied AI Build Stream, case-study template, maintenance/404, Cloudflare config; the public-repo README review ran in parallel (3 subagents).

---

## Output summary

**Phase 1 — foundation + homepage shell** (on `main`, `386efbd`)
- 15 static pages: Home, Case Studies (+4 dynamic cases), Journey, Lab, Internal Builds, Writing, Topics, About, Contact, `/under-maintenance`, 404.
- Typed content schema (§35): `cases` + `lab` collections (status, capabilities, evidence, milestones).
- Design system: dark-first tokens, one Matrix-green accent (§36, 80/15/5), restrained motion (§37).
- Matrix boot (§5), desktop pet (§14), unified status system (§38), Cloudflare Workers Static Assets config (§34).
- Gates: `astro build` → 15 pages · `astro check` → 0 errors, 0 warnings.

**Public-repo README review** → [Issue #1](https://github.com/F-e-u-e-r/portfolio/issues/1)
- 9 non-fork public repos audited vs the `ai-pet-usage` gold standard: per-repo issues + paste-ready descriptions + cross-cutting fixes + a house README template.
- P0: `tradingview-mcp` README identity bug (title + clone URLs point at the wrong repo `tradingview-mcp-review`; opening says 9 tools, ships 7); `opus-pack` version mismatch (README `alpha-0.1.16` vs latest tag `alpha-0.1.2`); `religion-council` 8 release titles still "Religion Council".
- Superseded 2026-09-24: issue #1 re-based onto `docs/PUBLIC_REPO_STANDARD.md`. The `opus-pack` "version mismatch" is a by-design decoupling (version of record is `plugin.json` `0.1.16`) — the fix is a canonical `v0.1.16` release, not a badge edit; the `religion-council` titles stay (historical names).

**Cross-model review fixes** → [PR #2](https://github.com/F-e-u-e-r/portfolio/pull/2) (merged 2026-09-23 23:48 via PR #4)
- 20 files, +94 / −31.

**Public-repo metadata standard** → [PR #5](https://github.com/F-e-u-e-r/portfolio/pull/5) (merged 2026-09-24 01:09)
- `docs/PUBLIC_REPO_STANDARD.md` v1 + review record (findings F1–F11, Opus Pack chronology, decisions D1–D10); issue #1 is the implementation checklist — steps 2–4 complete, steps 5–6 open.

**M1 — production baseline** → [PR #19](https://github.com/F-e-u-e-r/portfolio/pull/19) (merged 2026-09-24 18:56, `4258718`)
- 25 files, +1807 / −148 (1035 of them `package-lock.json`): route-state policy, page states, `robots.txt` + sitemap, dist gate, Playwright smoke, CI (`verify`, `e2e`, dormant `preview` / `deploy`), Worker Previews config.
- Gates: `astro check` 0 errors · `astro build` 15 pages · `verify-dist` 547/547 in both modes · Playwright 20 passed / 2 skipped · CI `verify` + `e2e` green on the PR and on `main`.

**M2 — automation hardening** → [PR #23](https://github.com/F-e-u-e-r/portfolio/pull/23) (merged 2026-09-25 01:55, `a44267e`)
- 9 files, +730 / −10: two Cloudflare gates (`CLOUDFLARE_PREVIEW_ENABLED` / `CLOUDFLARE_DEPLOY_ENABLED`), fail-closed Preview URL parser with an exact-hostname pin, Preview smoke (navigation-only custom 404, optional Access service token), 25 tool tests in the required `verify`, DECISIONS entries. Preceded by the M2 controlled Preview (2026-09-25 00:42–00:57): an assets-only Worker Preview works without a script; the Worker `ccso-portfolio` exists with no production deployment.
- Gates: `test:tools` 25/25 · `astro check` 0 errors · `verify-dist` 547/547 · Playwright 20 passed / 2 skipped · CI `verify` + `e2e` green on the PR and on `main`.

**Homepage simplification** → [PR #25](https://github.com/F-e-u-e-r/portfolio/pull/25) (merged 2026-09-25 13:30, `121c1df`)
- 3 files, +31 / −44: the About card, the "get in touch" CTA and the shared footer's contact card removed; footer = one-line identity + alias note + real contact links only (GitHub / Feuer · Email; LinkedIn once set) + navigation + legal; DECISIONS entry. The homepage ends on the Lab / Writing teaser.
- Gates: `astro check` 0 errors · `astro build` 15 pages · `verify-dist` 547/547 · `test:tools` 25/25 · Playwright 20 passed / 2 skipped · CI `verify` + `e2e` green on the PR and on `main`.

**M2 — account side, automated Preview validation, hold** → [PR #27](https://github.com/F-e-u-e-r/portfolio/pull/27) (merged 2026-09-25 14:39, `3f58db8`) + the M2 Preview hold pull request (`preview_urls = false`, DECISIONS entry)
- Validated: Cloudflare Access "Previews only" + service token (Phase A, 12:27) · Worker-scoped Editor token create / update / delete (Phase B, 12:43–12:46) · GitHub secrets + variables (14:20) · automated `pr-27` Preview on PR #27: create → exact-hostname pin → smoke 11/11 → PR comment, twice (update in place) · cleanup control-plane deletion (14:40).
- OPEN (provider): the deleted `pr-27` alias kept routing after the deletion and after Preview URLs were disabled → [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73). Automated Preview URLs disabled (`CLOUDFLARE_PREVIEW_ENABLED` removed 17:06; `preview_urls = false` in `wrangler.toml`). M2 not complete; launch path unblocked → M2.5.

**M2.5 — `workers.dev` production** → [PR #29](https://github.com/F-e-u-e-r/portfolio/pull/29) (merged 2026-09-25 22:30, `3c6a6a7`) + [PR #30](https://github.com/F-e-u-e-r/portfolio/pull/30) (merged 23:33, `12de99c`) — technical gate met 23:35 · completion record landed 23:50 ([PR #31](https://github.com/F-e-u-e-r/portfolio/pull/31), `10cd55e`)
- Production live on `https://ccso-portfolio.secc-biz.workers.dev` (pre-launch: `noindex, nofollow`, canonical `https://ccso.shsl.world/`, `PUBLIC_SITE_INDEXABLE` false / unset); repository variables `PRODUCTION_URL` + `CLOUDFLARE_DEPLOY_ENABLED=true` (22:25); production deploys on every push to `main` under `verify` → `e2e` → `deploy` → bounded readiness → `scripts/smoke-preview.sh` (PR #30: `needs: [verify, e2e]`, the inline `curl | grep -q` smoke replaced).
- Gates: initial acceptance 64/64 from the verified artifact (22:42) · final `main` run 36154921374 fully green (verify → e2e → deploy; Deploy SUCCESS, readiness 1/24 → 200, smoke 11/11) · Cloudflare `{enabled:true, previews_enabled:false}`, deployments / `deployed_on` populated · `CLOUDFLARE_PREVIEW_ENABLED` absent · no DNS / custom-domain change. The M2 Preview cleanup issue #73 stays OPEN, separately.

**M3A / M3B — DNS preflight + Custom Domain attachment, dual-host** → [PR #33](https://github.com/F-e-u-e-r/portfolio/pull/33) (merged 2026-09-26 01:03, `3c4890e`) + [PR #34](https://github.com/F-e-u-e-r/portfolio/pull/34) (merged 01:25, `686dd1f`) — technical gate met 01:27–01:29 · completion record landed 01:38 ([PR #35](https://github.com/F-e-u-e-r/portfolio/pull/35), `693019a`)
- M3A read-only: Path A — `shsl.world` already Cloudflare-authoritative (no nameserver migration), DNSSEC off, `ccso.shsl.world` NXDOMAIN, DNS / Google Workspace baseline captured. M3B: owner-attached Custom Domain (Dashboard); PR #33's `[[routes]]` failed trigger reconciliation on the Worker-scoped CI token (code 10084, no outage) → least-privilege recovery PR #34 removed it (Dashboard-managed Custom Domain = recorded exception, token not broadened); `main` run 36167004068 fully green.
- Gates: `https://ccso.shsl.world` 200 · valid TLS · smoke 11/11 · 24/24 files = artifact · `workers.dev` same content + smoke 11/11 · canonical `https://ccso.shsl.world/` · both `noindex, nofollow` · DNS / mail unchanged · Preview URLs disabled · robots.txt = build on both hosts (Bot Preference Sync off). Still pre-launch: `PRODUCTION_URL` = `workers.dev`, `PUBLIC_SITE_INDEXABLE` unset → M3C.

**M3C-1 — custom-domain production endpoint** → [PR #37](https://github.com/F-e-u-e-r/portfolio/pull/37) (merged 2026-09-26 02:20, `0041cec`) — technical gate met 02:21 · completion record landed 02:34 (this record's pull request)
- `PRODUCTION_URL` → `https://ccso.shsl.world` (02:11): the `deploy (production)` readiness poll and production smoke now observe the custom domain; `workers.dev` stays live as the rollback host only (`workers_dev = true`); `PUBLIC_SITE_INDEXABLE` absent (`noindex`) — M3C-2 follows the `Google-Extended` decision.
- Gates: `main` run 36172754060 fully green (verify → e2e → deploy; Deploy SUCCESS, Version `375ec462…`, deployment 10; readiness 1/24 → 200 on `https://ccso.shsl.world`; smoke 11/11) · dual-host read-back 02:23: both hosts 200, smoke 11/11, 24/24 files = artifact, canonical `https://ccso.shsl.world/`, both `noindex, nofollow`, `robots.txt` = build · Cloudflare `{enabled:true, previews_enabled:false}` · DNS / mail unchanged.

**M3C-1.5 — transport and WAF security baseline** (owner Dashboard mutations, 2026-09-26, accepted read-only 03:39 / 03:48 / 03:56) — completion record landed 04:12 (this record's pull request)
- Zone-wide HTTP→HTTPS 301 with the apex carve-out to `https://www.shsl.world/${1}` · Minimum TLS 1.2 · WAF custom rules (order: ports, then methods): edge ports other than 80 / 443 blocked and non-GET/HEAD blocked on `ccso.shsl.world` (except `/cdn-cgi/`) · no rate limiting · Bot Preference Sync off · DDoS protection + Free Managed Ruleset already active.
- Gates: G1 (ccso / www / apex chains, path + query preserved, one hop, no loop) · G2 (TLS 1.0 / 1.1 rejected, 1.2 / 1.3 accepted) · G3 + G4 (blocks = 403 `securitySource=firewallCustom`; `/cdn-cgi/rum` not blocked; port 80 → redirect, 443 → production) · production smoke 11/11 after every control · `workers.dev` healthy · DNS baseline identical.

**M3C-2 Step A — robots and AI-crawler content-use policy** → [PR #40](https://github.com/F-e-u-e-r/portfolio/pull/40) (merged 2026-09-26 15:35, `8ba42f1`) — technical gate met 15:37 · read-back 15:39 · landing record 15:53 (this record's pull request)
- 3 files, +35 / −17: `robots.txt` generator (six groups: `*`, `OAI-SearchBot`, `Claude-SearchBot` = `Allow: /` + `Disallow: /cdn-cgi/`; `GPTBot`, `ClaudeBot`, `Google-Extended` = `Disallow: /`; `Sitemap` line only when `PUBLIC_SITE_INDEXABLE=true`), dist gate (exact, ordered group-for-group comparison), DECISIONS entry. Owner decision: search / discovery allowed, general model-training reuse disallowed. Production stays pre-launch (`noindex, nofollow`, no Sitemap line).
- Gates: `astro check` 0 errors · `verify-dist` 549/549 in both modes · `test:tools` 25/25 · Playwright 20 passed / 2 skipped · dist identical to `main` `91c91f6` except `robots.txt` in both modes · 8 known-bad `robots.txt` mutations rejected by the new gate · CI `verify` + `e2e` green on the PR and on `main` · `main` run 36227263302 fully green (Version `45fa6168…`, readiness 1/24, smoke 11/11) · both hosts serve the six-group file byte-for-byte = artifact, asserted group-for-group · M3C-1.5 baseline + DNS re-validated unchanged.

**M3C-2 — public indexability cutover** → [PR #42](https://github.com/F-e-u-e-r/portfolio/pull/42) (merged 2026-09-26 16:21, `a53ba51`) — technical gate met 16:23 · acceptance 16:25 · completion record landed 16:32 (this record's pull request)
- Repository variable `PUBLIC_SITE_INDEXABLE=true` (16:16); the first launch-mode `main` deployment (run 36229515580, Version `62101731…`): live routes `index, follow` with self-canonical on `https://ccso.shsl.world`, placeholders / maintenance `noindex, nofollow`, 404 `noindex` without canonical, `robots.txt` = six groups + the canonical `Sitemap:` line, sitemap = live routes only (9 URLs at this build). `workers.dev` serves the same artifact (temporarily indexable, canonical → `ccso.shsl.world`) until M3D.
- Gates: PR `verify` + `e2e` green in launch mode · `main` run fully green (16 assets uploaded, readiness 1/24, smoke 11/11 with `index, follow`) · acceptance derived from the deployed artifact on both hosts (110 checks each) · M3C-1.5 baseline, Cloudflare state, variables, DNS re-validated unchanged.

**M3D — `workers.dev` retirement · M3 COMPLETE** → [PR #44](https://github.com/F-e-u-e-r/portfolio/pull/44) (merged 2026-09-26 17:09, `a01ea85`) — technical gate met 17:11 · acceptance 17:15 · completion record landed 17:24 (this record's pull request)
- 1 file, +1 / −1: `workers_dev = false` in `wrangler.toml` (`preview_urls = false` kept, no `[[routes]]`). Control plane `subdomain.enabled = false` on both APIs; `https://ccso-portfolio.secc-biz.workers.dev` answers Cloudflare's 404 `error code: 1042` on application paths (Cloudflare's platform robots.txt on `/robots.txt`); Wrangler now reports "No targets deployed" (the Custom Domain is Dashboard-managed). `ccso.shsl.world` = sole intended public production hostname; indexability contract, `robots.txt` + sitemap, canonical, G1–G4, DNS all unchanged. Version / Deployment URLs not claimed disabled; #73 independently OPEN.
- Gates: local `astro check` 0 errors · `verify-dist` launch 549/549 · `test:tools` 25/25 · token-less `wrangler deploy --dry-run` exit 0 · Playwright 20 passed / 2 skipped · CI `verify` + `e2e` green on the PR and on `main` · `main` run 36231935602 fully green (readiness 1/24, smoke 11/11) · retirement oracle proven with live / retired arms · M3C-1.5 baseline + DNS re-validated unchanged.

**M4-0a — public-baseline hygiene** → [PR #46](https://github.com/F-e-u-e-r/portfolio/pull/46) (merged 2026-09-26 19:50, `93ff043`) — technical gate met 19:52 · live read-back 19:55–19:58 · landing record landed 20:08 (this record's pull request)
- 3 files, +19 / −12: Contact renders only links with a real destination (the disabled "LinkedIn — Coming soon" / "Resume — View Resume" rows removed; rows return once set in `src/data/site.ts`; description "email and GitHub (Feuer)"), the Journey development note removed, DECISIONS entry. Owner-descoped from the four-item M4-0 AUTH: the LinkedIn link and the homepage Writing teaser → M4-0b once the URL is provided. Writing / Topics / Internal Builds remain In Preparation placeholders; case content, `/about/`, indexability, `robots.txt`, sitemap, Cloudflare and the M3C-1.5 baseline untouched.
- Gates: `astro check` 0 errors · build 15 pages · `verify-dist` 549/549 (launch) · `test:tools` 25/25 · Playwright 20 passed / 2 skipped · built `dist/` differs from the `c683a09` build only in `contact/` and `journey/` · CI `verify` + `e2e` green on the PR and on `main` · `main` run 36240090290 fully green (2 assets uploaded, Version `cd415cf1…`, readiness 1/24, smoke 11/11) · live `/`, `/journey/`, `/contact/` byte-identical to the artifact · `robots.txt`, sitemap, placeholder states and edge baseline unchanged.

**M4-1 — pure-evidence positioning hygiene + desktop refinement** → [PR #48](https://github.com/F-e-u-e-r/portfolio/pull/48) (merged 2026-09-27 02:04, `d9d6ea2`) — technical gate met 02:06 · live read-back 02:08 · landing record landed 02:26 (this record's pull request)
- 18 files, +161 / −121: the homepage Writing teaser removed (Home ends on one full-width Lab evidence block), public navigation = Case Studies · Journey · Lab · Contact as one list (About / Writing / Topics unlinked, every "Soon" gone), `/internal-builds/` removed (normal 404, 14 static pages now), `/writing/` promises no first article, Contact neutral (h1 "Contact", Email + GitHub / Feuer; `resume` field removed, `linkedin` optional), DECISIONS positioning / IA entry; desktop refinement measured before / after at three viewports: card text 14 → 15 px (`--text-md`), `--c-border` 0.12 → 0.16 (edge 1.20 → 1.31:1), `--c-accent-muted` for eyebrows / decorative metadata + hover / focus underline on text links, full-height page shell (short-page dead band 169 px → 0 at 1920×1080). Colours (all ≥ 5.2:1), fonts, container width, section rhythm and every content file unchanged.
- Gates: `astro check` 0 errors · build 14 pages · `verify-dist` 412/412 in both modes · `test:tools` 25/25 · Playwright 29 passed / 3 skipped (spec updated from the resulting IA; 13 assertions fail against the previous dist) · CI `verify` + `e2e` green on the PR and on `main` · `main` run 36261240380 fully green (16 files uploaded, Version `b2e6e5aa…`, readiness 1/24, smoke 11/11) · every live route byte-identical to the artifact · `/internal-builds/` 404 · `robots.txt`, sitemap, placeholder states and edge baseline unchanged.

---

## xcheck output (cross-model review of Phase 1)

**Reviewers:** `gpt-5.6-sol` + `gpt-5.6-luna` (`model_reasoning_effort=max`, via `codex exec`).
**Round 1:** complete. No high-severity defects. **11 unique findings** (4 raised by both), each reproduced before fixing:

| # | Sev | Finding | By | Status |
|---|-----|---------|----|--------|
| 1 | Med | `--c-fg-subtle` below WCAG AA (~3.9:1) | both | Fixed → `#828f88` (≥4.5:1 all surfaces) |
| 2 | Med | Desktop header nav wraps into two rows at ~901–999px | sol | Fixed → disclosure-menu breakpoint 900 → 1024px |
| 3 | Med | `tsconfig baseUrl` deprecated → `tsc` TS5101 | sol | Fixed → removed unused `baseUrl` + `paths` |
| 4 | Low | No `aria-current`; mobile links lack active state | both | Fixed |
| 5 | Low | Listing pages skip a heading level (h1 → h3) | sol | Fixed → `CaseCard` `h2` on listings; Lab `h2` |
| 6 | Low | Build-stream milestones not exposed to screen readers | luna | Fixed → `sr-only` milestone text |
| 7 | Low | Boot overlay fades (420ms) on repeat visits | luna | Fixed → instant removal |
| 8 | Low | `summary_large_image` but no `og:image` | both | Fixed → `public/og.png` (1200×630) + tags |
| 9 | Low | All 9 section pages share one meta description | sol | Fixed → per-page descriptions |
| 10 | Low | `prefetch` config inert (no link opts in) | both | Fixed → `prefetchAll: true` |
| 11 | Low | Maintenance page has no `h1` | luna | Fixed |

**Not a defect:** both reviewers' sandboxed `astro check` / `tsc --noEmit` failed only because the review sandbox was read-only (could not write `.astro/`); both gates pass cleanly in the normal environment.

**Round 2 (re-review of the fix diff):** **quota-blocked** — the OpenAI/Codex account hit its usage limit mid-run (resets **2026-09-24 ~01:00 UTC+8**), so no PROCEED/FIX verdict was returned; **none is inferred**. Round-1 fixes stand on execution-based verification (build, check, contrast recomputation, 960px header render). Re-run available after the quota resets.

> Reviewer-family note: `sol` and `luna` are two variants of one provider family (OpenAI GPT-5.6); both differ from the author family (Claude Opus 4.8), so this is a cross-family gate on the author, not two independent families.

### Grok 4.6 (high) — 2 rounds

Grok 4.6 (high) ran twice via `xcheck --grok`, each on two packets (the prior GPT review + PR #2 as context; and the site's structure / content / design). Grok is a live principal (not OS-sandboxed), so it ran review-only on a clean tree — the tree was verified untouched after each round. Grok found deeper product / IA / design issues than the GPT pass.

**Round 1** — 7 findings (no high-severity): nav stubs, boot FOUC (deferred-module flash + per-tab replay), unlinked content graphs, evidence-in-template, positioning, homepage density, Worldview repo name. Applied: Topics Soon, blocking head-script boot skip + localStorage, Lab-teaser StatusBadge, evidence de-dup, brand, mobile dots + pet clearance.

**Round 2** — validated round 1 and **caught a regression**: the longer brand + Soon pills re-opened the header wrap (~1025–1090px, measured). Applied: desktop header trimmed to 5 primary sections (Soon routes → footer + mobile menu; no wrap 861–1101px+); evidence honesty completed on cards + case pages (empty-state note); footer Soon badges; first-visit boot made keyboard/AT-safe (rest of page `inert`); hero alias dropped.

**Deferred with reasons** (not fixed this round): content-graph refactor (buildStream ↔ collections, lab detail pages) vs frozen scope §10/§27; journey roles / About answered fields / more transformation cases (need real career content, MVP pass); 10-section homepage (frozen §4); `religion-council` rename (held issue #1); web fonts + robots/sitemap/JSON-LD (at deploy).

---

## Next

- **Step 2 — COMPLETE.** `portfolio` PR #6 and the TradingView README truth-fix PR merged; `tradingview-mcp` made private and removed from public-repository / portfolio scope.
- **Step 3 — COMPLETE.** A: `skills` `v0.1.16` pre-release (tag on merge commit `df1248e`); B/C: StarLedger `v1.3.0-alpha.1` pre-release + `v1.2.0-alpha.1` title clarified, StarLedger Template `v1.2.0-alpha.1` pre-release; D/E: no action by design (religion-council's next genuine release carries "Worldview Council"; ai-pet-usage stays on its governed exception — migration ticket when that work is scheduled).
- **Step 4 — COMPLETE.** 4a descriptions + topics on all 9 public repos; 4b lifecycle badges on all 8 Tier-A repos (8/8 read back from `main`); solo-maintained rulesets = PR + required CI, 0 approving reviews; `ai-pet-usage` gained its required check; the StarLedger template builder repaired at the source and the template resynchronised.
- **Step 5 — repository track, now parallel to the launch critical path.** Decisions frozen (D11–D18). Done: ai-arena ✓ (`v0.1.0`, PR #28) · religion-council ✓ (PR #64) · starledger ✓ (PR #290, `e4ec5b7`) · starledger-template ✓ (PR #5, `ff93cbe`). Remaining as a parallel repository-hygiene track (owner roadmap, 2026-09-24): portfolio / homebrew-tap licensing → bilingual filenames → forks → step 6 extensions. One repository per cycle: diff proposal → PR → CI → merge → release.
- **Launch critical path (owner roadmap, 2026-09-24):** Deploy Readiness Audit ✓ → CI + test baseline ✓ → page states (custom 404 · In Preparation placeholder · Under Maintenance) ✓ → SEO metadata / robots / sitemap ✓ (**M1 COMPLETE**, PR #19, 18:56) → `main` ruleset ✓ (19:07) → artifact actions pinned to Node 24 ✓ (PR #21, 19:50) → `e2e` required ✓ (19:56 — CI governance closed: pull request → `verify` + `e2e` required → merge commits only → 0 approving reviews) → M2 controlled Worker Preview ✓ (2026-09-25 00:42–00:57: assets-only Preview without a script; Worker `ccso-portfolio` exists, no production deployment) → M2 automation hardening ✓ (PR #23, 01:55: `CLOUDFLARE_PREVIEW_ENABLED` / `CLOUDFLARE_DEPLOY_ENABLED` split, fail-closed parser + hostname pin, Preview smoke, `test:tools` in `verify`) → M2 account side ✓ (2026-09-25: Access "Previews only" + Phase A 12:27, Editor token Phase B 12:43–12:46, GitHub secrets + variables 14:20) → automated Preview validation PR #27 ✓ (create / exact-host pin / smoke / comment green twice; cleanup control-plane deletion green, 14:40) → **M2 cleanup / routing provider issue OPEN** (the deleted `pr-27` alias kept routing after the deletion and after Preview URLs were disabled — [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73); automated Preview URLs disabled: `CLOUDFLARE_PREVIEW_ENABLED` removed 17:06, `preview_urls = false` in `wrangler.toml`; **M2 not complete**, launch path unblocked) → M2.5 `workers.dev` production ✓ (2026-09-25: PR #29 first deploy 22:31, acceptance 64/64 22:42, PR #30 smoke hardening 23:33, `main` run 36154921374 fully green — technical gate met 23:35 · completion record landed 23:50 (PR #31); verify → e2e → deploy → readiness → production smoke 11/11; subdomain `{enabled:true, previews_enabled:false}`; **M2.5 COMPLETE**) → M3A DNS preflight ✓ (2026-09-26 00:20–00:35, read-only: Path A — `shsl.world` already Cloudflare-authoritative, no nameserver migration, DNSSEC off, `ccso.shsl.world` NXDOMAIN, DNS / Google Workspace baseline captured) → M3B Custom Domain ✓ (owner Dashboard attachment; PR #33 repo-managed attempt failed trigger reconciliation on the Worker-scoped token — code 10084, no outage; least-privilege recovery PR #34 `686dd1f`; `main` run 36167004068 fully green; dual-host validated 01:29 — technical gate 01:27–01:29 · record landed 01:38 (PR #35); **M3B COMPLETE**, still pre-launch `noindex`) → M3C-1 production endpoint cutover ✓ (2026-09-26: `PRODUCTION_URL` → `https://ccso.shsl.world` 02:11; PR #37 02:20; `main` run 36172754060 fully green with readiness + smoke against the custom domain — technical gate 02:21 · record landed 02:34 (this record's pull request); `workers.dev` live as rollback only; **M3C-1 COMPLETE**) → M3C-1.5 security baseline ✓ (2026-09-26: HTTPS enforcement + apex carve-out, Minimum TLS 1.2, two host-scoped WAF custom rules, no rate limiting — accepted 03:48 · record landed 04:12; **M3C-1.5 COMPLETE**) → `Google-Extended` decision ✓ (owner, 2026-09-26: disallow — search / discovery allowed, general model-training reuse disallowed) → M3C-2 Step A robots / AI-crawler policy ✓ (PR #40 15:35, `main` run 36227263302 fully green; six-group `robots.txt` live on both hosts, no Sitemap line, pages still `noindex` — technical gate 15:37 · record landed 15:53 (this record's pull request); **M3C-2 Step A COMPLETE**) → M3C-2 Step B public indexability cutover ✓ (2026-09-26: `PUBLIC_SITE_INDEXABLE=true` 16:16; PR #42 16:21; `main` run 36229515580 fully green in launch mode — technical gate 16:23 · acceptance 16:25 · record landed 16:32 (this record's pull request); live routes `index, follow`, sitemap advertised, placeholders / maintenance / 404 non-indexable; **M3C-2 COMPLETE**; `secc.studio` fully deferred: no DNS change, no redirect) → M3D `workers.dev` retirement ✓ (2026-09-26: `workers_dev = false`, PR #44 17:09; `main` run 36231935602 fully green — technical gate 17:11 · acceptance 17:15 · record landed 17:24 (this record's pull request); control plane `enabled = false`, the hostname answers Cloudflare's 404 `error code: 1042`; Version / Deployment URLs not claimed disabled, #73 OPEN; **M3D COMPLETE · M3 COMPLETE**) → M4 scope review ✓ (2026-09-26 evening, read-only proposal: baseline = the four featured cases de-placeholdered, a short About live, Contact honesty, the Journey note; Writing / Topics / Internal Builds deferred) → M4-0a public-baseline hygiene ✓ (PR #46 19:50, `main` run 36240090290 fully green — technical gate 19:52 · record landed 20:08 (this record's pull request); Contact dead rows + Journey note removed) → M4-1 pure-evidence positioning hygiene + desktop refinement ✓ (2026-09-27: PR #48 02:04, `main` run 36261240380 fully green — technical gate 02:06 · live read-back 02:08 · record landed 02:26 (this record's pull request); Writing teaser and every "Soon" gone, About / Writing / Topics unlinked, Internal Builds removed, Contact neutral, full-height page shell, measured desktop readability pass; M4-0b superseded — LinkedIn and a public resume are not v1 requirements) → **next: M4-2 Worldview Council + StarLedger case content → M4-3 Sales-to-Ops → M4-4 HK Employment RAG → M4-5 About short version + `/about/` live (navigation item restored) → M4-6 baseline gate + record; Writing returns only with published articles, Topics stay deferred (separate AUTH each; Search Console submission and the Public Repo Final Consistency Pass also separate)**. Milestones M1 technically deployable ✓ → M2 / M2.5 pre-launch production ✓ (Preview automation on hold) → M3 public MVP ✓ (2026-09-26 17:24) → M4 full v1. `e2e`-green pull requests: 3/3 (#19, #20, #21); `e2e` required since 19:56.
- Re-run xcheck round 2 after the quota resets.
- MVP content pass (Phase 2): real RAG + Sales-to-Ops case content, journey milestones, About; web fonts still open (no loading defect today); LinkedIn + resume dropped as v1 requirements by the 2026-09-27 positioning decision.
- Deployed to Cloudflare `workers.dev` (M2.5 ✓) and `ccso.shsl.world` attached as a Dashboard-managed Custom Domain (M3B ✓); since M3C-1 ✓ the custom domain is the CI-observed production endpoint (`PRODUCTION_URL`) and, since M3D ✓ (2026-09-26), the production `workers.dev` route is retired (`workers_dev = false`; the hostname answers Cloudflare's 404 `error code: 1042`) — `ccso.shsl.world` is the sole intended public production hostname; automated Worker Previews on hold pending [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73); edge security baseline in place since M3C-1.5 ✓ (HTTPS-only, TLS ≥ 1.2, GET/HEAD-only, ports 80/443 only); AI-crawler content-use policy live since M3C-2 Step A ✓ (PR #40: `Google-Extended` disallowed, `/cdn-cgi/` excluded in every allow group, exact dist gate); **publicly indexable since M3C-2 ✓** (`PUBLIC_SITE_INDEXABLE=true`, 2026-09-26: live routes `index, follow`, `robots.txt` advertises `https://ccso.shsl.world/sitemap-index.xml`, placeholders / maintenance / 404 stay non-indexable; `workers.dev` temporarily indexable with canonical → `ccso.shsl.world` until M3D — retired 2026-09-26); **M3 COMPLETE**; Version / Deployment URLs not claimed disabled, #73 independently OPEN; M4-0a public-baseline hygiene landed 2026-09-26 20:08 (PR #46, `93ff043`: Contact dead rows + Journey note removed); M4-1 pure-evidence positioning hygiene + desktop refinement landed 2026-09-27 02:26 (PR #48, `d9d6ea2`: no unfinished signals in the public chrome, public navigation = Case Studies · Journey · Lab · Contact, Internal Builds removed, full-height page shell, measured readability pass); next the M4 case-content cycles (M4-2 onwards), separate AUTH each. The deploy-readiness items from `docs/DECISIONS.md` (`robots.txt` + sitemap, `noindex` on maintenance / placeholder pages, AI-crawler policy) landed in M1.
