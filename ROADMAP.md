# Roadmap & Changelog

Progress log for the Eric So portfolio (`ccso.shsl.world`). Timestamps in **UTC+8 (Asia/Hong_Kong)**, newest first.

Scope is frozen at **v1.0** — rationale in [`docs/DECISIONS.md`](docs/DECISIONS.md). The v1.0 scope defines Phases 1–7; this file tracks what has actually shipped.

---

## What we've done

### 2026-09-25

- **23:50 — M2.5 `workers.dev` production deployment — COMPLETE record landed** — technical acceptance gate met at 23:35 HK on `main` run 36154921374; the completion record landed at 23:50 via PR #31 (merge `10cd55e`). Production is live on `https://ccso-portfolio.secc-biz.workers.dev` (temporary host until M3; every page `noindex, nofollow`, canonical `https://ccso.shsl.world/`, `PUBLIC_SITE_INDEXABLE` unset → `false`). Initial production deployment cycle: PR #29 (merge `3c6a6a7`, 22:30) was the first push to `main` with `CLOUDFLARE_DEPLOY_ENABLED=true`; its run 36147964561 deployed successfully (Wrangler 4.137.0, Version `0f0d862c…`, the `workers.dev` URL as the only trigger) while the old inline `Production smoke` step went red twice with the Wrangler deployment green both times — attempt 1 on the very first request, 0.4 s after the route was enabled (no readiness wait); attempt 2 (`gh run rerun --failed`, Version `17ec3c7d…`) deterministically on `curl … | grep -q` under `pipefail` (curl exit 23, confirmed by local reproduction with PIPESTATUS controls). Initial production acceptance at 22:42 from an anonymous client against the verified CI artifact (byte-identical to the local build): 64/64 — `scripts/smoke-preview.sh` 11/11, all 24 `dist/` files byte-identical on the live host, `/about/` placeholder contract (200 + `noindex, nofollow`, excluded from the sitemap), `/case-studies` 307 → `/case-studies/`, navigation 404 = `ROUTE_NOT_FOUND`, robots meta and canonical as above. CI hardening: PR #30 (merge `12de99c`, 23:33; `ci.yml` only) — `deploy (production)` `needs: [verify, e2e]`, a bounded readiness wait (24 × 5 s, per-request timeouts, `::error::` on exhaustion), then the shared `scripts/smoke-preview.sh`. Final accepted `main` run **36154921374** (attempt 1, fully green): `verify` SUCCESS → `e2e` SUCCESS → `deploy (production)` started only after both; Deploy SUCCESS (Version `bd091066…`, production deployment 3); readiness 1/24 → 200; production smoke 11/11; job SUCCESS. Cloudflare read-back: subdomain `{enabled:true, previews_enabled:false}` — the production `workers.dev` route is on while Preview / Version URL routing stays off; production deployments and `deployed_on` populated. `CLOUDFLARE_PREVIEW_ENABLED` remains absent (automated Previews disabled; the Preview cleanup / routing issue [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73) stays OPEN and independent of production acceptance); no DNS or custom-domain work has occurred. Steady state from here: every push to `main` deploys production through `verify` → `e2e` → `deploy` → readiness → production smoke. Next launch milestone: **M3 — `ccso.shsl.world` DNS migration + custom domain** (separate AUTH).
- **22:25 — M2.5 started: production target configured; the merge of this pull request is the first push to `main` with the production deploy enabled** — preflight on clean `main` `b6558ad` (PR #28 merged, local = origin): `wrangler.toml` `workers_dev = true`, `preview_urls = false`, `previews = { }`; repository variables before this cycle: `CLOUDFLARE_WORKERS_SUBDOMAIN=secc-biz` only (`CLOUDFLARE_PREVIEW_ENABLED` absent — automated Previews stay on hold; `CLOUDFLARE_DEPLOY_ENABLED` absent); the four Cloudflare secrets present by name; Cloudflare read-back with the Worker-scoped token: Worker `ccso-portfolio` 0 deployments, 0 versions, 0 Previews, `deployed_on: null`, subdomain `{enabled:false, previews_enabled:false}`, production `https://ccso-portfolio.secc-biz.workers.dev/` → 404 `error code: 1042` (its `/robots.txt` still Cloudflare's platform "content signals" text — no Worker serves the host yet); Wrangler 4.137.0 unchanged; the old `pr-27` alias neither probed nor mutated. Local gates on `b6558ad`: `astro check` 0 errors / 0 warnings, `astro build` 15 pages, `verify-dist` 547/547, `test:tools` 25/25, Playwright 20 passed / 2 skipped. Set at 22:25 and read back (`gh variable list` + per-variable GET): `PRODUCTION_URL=https://ccso-portfolio.secc-biz.workers.dev` (the `deploy` job's production smoke target) and `CLOUDFLARE_DEPLOY_ENABLED=true`. Untouched: `CLOUDFLARE_PREVIEW_ENABLED` (still absent), Access policies, DNS, custom domains, `PUBLIC_SITE_INDEXABLE` (unset → CI default `false`: every page stays `noindex, nofollow`, canonical stays `https://ccso.shsl.world/`). This pull request changes no code: on the PR `verify` + `e2e` run while `preview` and `deploy` are skipped; the `main` run after the merge is expected to execute `deploy (production)` — `wrangler deploy` of the artifact that passed `verify` (expected to enable the production `workers.dev` route through `workers_dev = true` while enforcing `preview_urls = false`), then the production smoke against `PRODUCTION_URL`. M2.5 is recorded complete only after the production acceptance checks pass — separate record.
- **21:31 — M2 cleanup / routing provider issue reported upstream; durable `preview_urls = false` hardening lands with this pull request** — [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73) filed with the non-secret evidence only (Static Assets Worker, Wrangler 4.137.0, Preview `pr-27`: automated create / update succeeded, `wrangler preview delete --name pr-27 --skip-confirmation` succeeded, cleanup run succeeded, Preview list empty, GET by id → 10025, owner disabled Preview URLs, API `enabled=false` / `previews_enabled=false`, alias still 302 anonymous / 200 authenticated 107 min after the toggle and 4 h 13 min after the deletion, never-requested route and navigation 404 prove live routing, production undeployed throughout, controls inactive within ~30–55 s; documentation mismatch quoted — `preview delete` "Delete a Preview and all its deployments", "Disabling Version URLs disables routing to Version URLs and aliased Version URLs"; asking expected lifecycle / delayed propagation / routing bug). This pull request makes the hold durable in the repository: `wrangler.toml` `preview_urls = false` (explicit — the file said `true`, which the next `wrangler deploy` would have pushed back to the account over the dashboard "off"; Cloudflare documents that an omitted key leaves the existing setting untouched but states no intent), `workers_dev = true` unchanged for M2.5, the empty `previews = { }` block kept (the required Preview configuration namespace, independent of the routing switch); `docs/DECISIONS.md` gained "2026-09-25 — M2 Preview hold". Untouched: `CLOUDFLARE_DEPLOY_ENABLED` (absent), the GitHub secrets, `CLOUDFLARE_WORKERS_SUBDOMAIN`, Access policies, DNS, custom domains; no Preview or production deployment in this cycle (local gates only: `astro check`, build, `verify-dist`, `test:tools`, Playwright against `wrangler dev`, which parses the new config). State: M2 core automation validated · M2 cleanup / routing provider issue OPEN · automated Preview URLs disabled · M2 not complete · launch path unblocked → next operational milestone M2.5 `workers.dev` production deployment (separate AUTH).
- **17:06 — M2 ON HOLD: the Preview cleanup acceptance gate failed; automated Previews disabled** — PR #27 merged 14:39 (`3f58db8`; `verify` + `e2e` + `preview (pr-27)` green on both runs, `deploy` skipped) and `preview-cleanup` run 36103863659 succeeded at 14:40 (`wrangler preview delete --name "pr-27" --skip-confirmation` → "✨ Preview "pr-27" deleted successfully."; Cloudflare Preview list empty; `GET …/previews/76618c57…` → 10025 "Preview not found") — control-plane deletion OK. Yet the named alias `https://pr-27-ccso-portfolio.secc-biz.workers.dev` kept routing: anonymous → 302 to the Access login, service token → 200 with the full site, sampled every 20 s for 35 min, again after a 6-minute quiet window (15:21), on the never-requested `/journey/` (15:22) and with the Worker's own `ROUTE_NOT_FOUND` page on an unknown navigation route ⇒ live routing, not a cache; the two Phase B aliases deleted the same way had gone inactive within ~30–55 s (still 404 at 14:53). Owner decision ("AUTH: hold M2 and disable automated Previews"): the cleanup gate FAILED; repository variable `CLOUDFLARE_PREVIEW_ENABLED` deleted (read back: not found; `CLOUDFLARE_WORKERS_SUBDOMAIN=secc-biz` unchanged; the four secrets present by name; `CLOUDFLARE_DEPLOY_ENABLED` absent) → `preview` job and `preview-cleanup.yml` dormant; the owner also disabled the Worker's Version / Preview URLs in the dashboard (17:06:37, Worker `updated_on`). Final read-only probe 18:53:47 (107 min after the toggle, 4 h 13 min after the deletion): API `{enabled:false, previews_enabled:false}`; alias anonymous → 302, with the service token → 200 (29 926 B homepage, `x-robots-tag: noindex`); production `workers.dev` 404 `error code: 1042`, 0 deployments, `deployed_on: null`; no polling afterwards. M2 is NOT complete: the core automation is validated, the cleanup / routing behaviour is a provider issue to report upstream (evidence preserved outside the repository: workflow logs, API read-backs, probe outputs).
- **14:20 — M2 final cycle started: GitHub Actions secrets and variables configured; automated pull-request Preview validation being exercised by this PR** — repository secrets `CLOUDFLARE_API_TOKEN` (the Worker-scoped Editor token validated in Phase B), `CLOUDFLARE_ACCOUNT_ID`, `CF_ACCESS_CLIENT_ID`, `CF_ACCESS_CLIENT_SECRET` (set with `gh secret set --repo`, values fed from the owner's mode-600 local file through stdin, never echoed; names verified with `gh secret list`) and repository variables `CLOUDFLARE_WORKERS_SUBDOMAIN=secc-biz`, `CLOUDFLARE_PREVIEW_ENABLED=true`; `CLOUDFLARE_DEPLOY_ENABLED` deliberately absent (production deploy stays off until M2.5) and `PUBLIC_SITE_INDEXABLE` unset (CI default `false`, pre-launch). **M2 is not complete**: this pull request is the first automated Preview (`pr-<number>`); its Preview job, Preview smoke, comment and the post-merge cleanup are observed before M2 is recorded complete. **Observed 14:30–14:35 on PR #27, head `14d400e` (CI run 36103016463):** required `verify` and `e2e` SUCCESS; `preview (pr-27)` SUCCESS — create + exact-hostname pin, then `Preview smoke` 11/11 against `https://pr-27-ccso-portfolio.secc-biz.workers.dev` with the service token (06:29:50–51Z), then the pull-request comment (06:29:52Z, `github-actions[bot]`, "built from 14d400e…") — the URL was posted only after the smoke passed; `deploy (production)` SKIPPED. Independently from a local client: anonymous `/` (bare and navigation) and `/robots.txt` → 302 to the Access login; with the Access service token → 200, `scripts/smoke-preview.sh` 11/11, `/`, `/robots.txt`, `/sitemap-index.xml`, `/about/` byte-identical to the local `dist/`, `x-robots-tag: noindex`; Cloudflare Preview list = `pr-27` only; production `workers.dev` 404 `error code: 1042`, 0 deployments, `deployed_on: null`; `CLOUDFLARE_DEPLOY_ENABLED` absent. This evidence commit updates the same `pr-27` Preview in place.
- **13:30 — Homepage simplified: the site ends on evidence, not a conversion funnel** — PR #25 merged with a merge commit (`121c1df`; `verify` + `e2e` green on the PR run 36098691386 and again on `main` run 36098839836, no bypass; `preview` / `deploy` skipped — no Cloudflare mutation in this cycle). Owner design decision: the homepage reads as a senior professional's curated evidence portfolio, not a consulting / lead-generation landing page. Removed from `src/pages/index.astro`: the About teaser card (`identity.thesis` + "More about me") and the "Explore the case studies, or get in touch." CTA section, with their scoped CSS — the page now ends on the Lab / Writing teaser (Hero, capability strip, Featured case studies, Journey, Capability → Evidence, Working principles unchanged). Removed from the shared `src/components/SiteFooter.astro`: the "Have a problem worth solving well?" contact card (it was every page's ending, so the removal is site-wide; a homepage-only layout prop was rejected); the footer identity block is now one line (`identity.name — identity.role`) + the alias note + lightweight links built from `src/data/site.ts`, rendering only links with a real destination (GitHub / Feuer · Email today; LinkedIn appears once `contact.linkedin` is set — owner review adjustment applied before push: no "Soon" placeholder in the shared footer); footer navigation and the legal line unchanged; the footer no longer carries a heading. `docs/DECISIONS.md` gained "2026-09-25 — Homepage ending: evidence, not a conversion funnel". Evidence on the PR head `b7a8e6f`: `astro check` 0 errors / 0 warnings, `astro build` 15 pages, `verify-dist` 547/547, `test:tools` 25/25, Playwright 20 passed / 2 skipped; rendered against `wrangler dev`: homepage 5608 → 4586 px (desktop 1280) and 8102 → 6901 px (mobile 412), 9 → 7 `main` sections, footer 583 → 317 px, headings h1 → h2 → h3 with no level skips, one `mailto` and one GitHub link left (footer), About / Contact reachable from header + footer navigation, section rhythm before the footer unchanged. Read back from `main` by an unauthenticated client after the merge: `src/pages/index.astro`, `src/components/SiteFooter.astro`, `docs/DECISIONS.md` byte-identical to the PR head.
- **12:43–12:46 — M2 Phase B: Worker-scoped Editor API token validated (create / update / delete)** — every Wrangler command ran with `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` from the owner's local file under an isolated Wrangler config directory (control arm without the token → "not authenticated", so no fallback to the owner's OAuth login was possible; `wrangler whoami` → "Account API Token … read from the CLOUDFLARE_API_TOKEN environment variable"). `npx wrangler preview --name m2-token-smoke --json` → Preview `b900835b…`, Deployment 1 `70611a92…`; `scripts/extract-wrangler-preview-url.mjs --expect-hostname m2-token-smoke-ccso-portfolio.secc-biz.workers.dev` accepted the URL; anonymous → 302 to the Access login on the named and the immutable Deployment URL (a bogus service token gets the same 302); with the Access service token → `scripts/smoke-preview.sh` 11/11 and `/`, `/robots.txt`, `/sitemap-index.xml`, `/about/` byte-identical to `dist/` on both hosts, canonical `https://ccso.shsl.world/`, robots `noindex, nofollow`, platform `x-robots-tag: noindex`. A second run of the same command updated the Preview in place (same Preview id, Deployment 2 `fe6b8715…`, message annotation recorded). `wrangler preview delete --name … --skip-confirmation` removed `m2-token-smoke` and then `m2-access-smoke` (one at a time; API list empty, `GET …/previews/<id>` → 10025 "Preview not found"). After deletion: both named hosts → anonymous 302, service token 404 (inactive); the immutable Deployment URLs `70611a92`, `fe6b8715`, `e64693b4` and the 00:42 smoke's `fe534f76` (its Preview deleted ~12 h earlier) → anonymous 302, service token 200 with the full site, unchanged 17 minutes later — a live deployment, not a cache (`/nope-<ts>/` returns the Worker's own 9-byte 404 body; `cf-cache-status: HIT` is a Static Assets platform header on every response). Production `workers.dev` still 404 `error code: 1042`, 0 deployments, subdomain `{enabled:false, previews_enabled:true}`. Owner verdict: PASS — local least-privilege Preview capability validated; `preview delete` ≠ Deployment URL revocation stands, with Access as the control.
- **12:27 — M2 Phase A: Cloudflare Access "Previews only" validated on the owner-OAuth Preview `m2-access-smoke`** — account side (owner, Cloudflare dashboard, ~10:35–12:02): Zero Trust team `s-e-c-c`; Access application on `*-ccso-portfolio.secc-biz.workers.dev` with a human (e-mail) policy and a Service Auth policy for a service token; Worker `ccso-portfolio` subdomain flags `enabled: false` (production `workers.dev` off) and `previews_enabled: true`; the service token and the Worker-scoped Editor API token kept in a mode-600 local file outside the repository (`CF_ACCESS_CLIENT_ID` / `CF_ACCESS_CLIENT_SECRET` / `CLOUDFLARE_API_TOKEN`; the Client Secret follows Cloudflare's new `cfast_…` format). Observed: every `*-ccso-portfolio.secc-biz.workers.dev` host — named, Deployment, deleted, even nonexistent names — answers 302 → `https://s-e-c-c.cloudflareaccess.com/cdn-cgi/access/login/<host>` to anonymous requests (Access matches the wildcard before any Worker lookup), while production `ccso-portfolio.secc-biz.workers.dev` stays 404 `error code: 1042`; with the service token the named `m2-access-smoke-…` and the immutable `e64693b4-…` Deployment URL both pass `scripts/smoke-preview.sh` 11/11 with `/`, `/robots.txt`, `/sitemap-index.xml`, `/about/` byte-identical to `dist/` (a first attempt at 12:05 was rejected until the owner fixed the Access side — the credential file was unchanged). The owner's browser login through Access passed afterwards (human policy). Also learned: on a host no Worker serves, `/robots.txt` returns Cloudflare's platform "content signals" text (1248 B), a platform artifact, not a deployment; the Worker's own 165-byte `robots.txt` wins wherever the Worker serves.
- **01:55 — M2 automation hardening landed** — PR #23 merged with a merge commit (`a44267e`; `verify` + `e2e` green on the PR run 36037305616 and again on `main` run 36037674763, no bypass; `preview` / `deploy` skipped — both gates unset, no Cloudflare mutation in this cycle). Landed: two independent gates — `CLOUDFLARE_PREVIEW_ENABLED` (pull-request Previews + `preview-cleanup.yml`) and `CLOUDFLARE_DEPLOY_ENABLED` (production deploy on `main`) — so enabling PR Previews can no longer production-deploy the next merge; `scripts/extract-wrangler-preview-url.mjs` (fail-closed Preview URL extraction from Wrangler's mixed stdout; `--expect-hostname` accepts only exactly `pr-<n>-ccso-portfolio.<CLOUDFLARE_WORKERS_SUBDOMAIN>.workers.dev` before any credential follows the URL) with the real controlled-Preview output as the regression fixture (author fields redacted, subdomain replaced by `example-subdomain`); `scripts/smoke-preview.sh` (curl: 200s, real 404 for any unknown route, the `ROUTE_NOT_FOUND` page on a browser navigation, 404 page `noindex` without canonical, `robots.txt` / `sitemap-index.xml`, trailing-slash redirect, canonical, robots meta per `PUBLIC_SITE_INDEXABLE`; optional Cloudflare Access service token `CF_ACCESS_CLIENT_ID` + `CF_ACCESS_CLIENT_SECRET`, both or neither — exactly one refuses before any request); preview job order create → extract + pin → smoke → comment; `npm run test:tools` (25 tests, including a loopback mock that records every request the smoke makes) in the required `verify` job after `test:site` — same check context, no ruleset change; `docs/DECISIONS.md` section "2026-09-25 — M2 automation hardening". Owner review adjustments applied before push: subdomain redacted from the fixture, `test:tools` moved into `verify` (not repeated in the preview job), smoke before the comment, Access service-token support, exact-hostname pin. Read back from `main` by an unauthenticated client: all nine files byte-identical to the merge; `verify` on `main` ran `test:tools` (step 8, success).
- **00:42–00:57 — M2 controlled Worker Preview (first authenticated Cloudflare run; owner OAuth login, local only)** — on clean `main` `36a53cf` (Wrangler 4.137.0; `npm ci` / `build` / `test:site` exit 0): `npx wrangler preview --name m2-smoke --json` succeeded on the assets-only project **without a script** — Wrangler synthesised `no-op-worker.js` as the `main_module`, uploaded 24 assets and created the Worker `ccso-portfolio` (before: `deployments list` → "This Worker does not exist" 10007, identical to a nonexistent-name control; after: `[]` — no production deployment or version; the production `workers.dev` host answers 404 `error code: 1042`). Read back unauthenticated on the Preview URL (`m2-smoke-ccso-portfolio.<subdomain>.workers.dev`) and on the immutable Deployment URL: `/` 200 and byte-identical to `dist/index.html`; `robots.txt` / `sitemap-index.xml` 200 and byte-identical, no `Sitemap:` line; `/case-studies` 307 → `/case-studies/`; canonical `https://ccso.shsl.world/`; every live, placeholder and maintenance route `noindex, nofollow`; unknown routes 404. Three findings: (1) the edge serves `dist/404.html` only to navigation requests (`Sec-Fetch-Mode: navigate`; a bare curl gets a 9-byte `Not found`, still 404) while `wrangler dev` serves it to every client — confirmed with the header as the only variable and by the compatibility flag `assets_navigation_prefers_asset_serving`; (2) `wrangler preview --json` prints the asset-upload progress to stdout before the JSON, so the `jq` parser in `ci.yml` failed on the captured output (the key in use is `.preview.urls[0]`); (3) every Preview response carries a platform `x-robots-tag: noindex`. Deleted with `wrangler preview delete --name m2-smoke --skip-confirmation` (exit 0; the Preview URL then answers Cloudflare's 404 page; the Worker remains) — the immutable Deployment URL was still serving the site an hour later. Owner decisions (01:10): core PASS, M2 not yet complete; no architecture change for the 404; `preview delete` ≠ Deployment URL revocation, so Access "Previews only" precedes automation; `X-Robots-Tag` is evidence, not contract; the parser is a fail-closed helper, not `sed`.

### 2026-09-24

- **19:56 — `e2e` promoted to a required check; CI governance closed** — `protect-main` updated through the API (the ruleset update endpoint): `required_status_checks` = `verify` + `e2e`, both from the GitHub Actions app, `strict: false` unchanged; every other field unchanged, confirmed by a structural diff of the full read-back against the pre-change state (the only difference is the added `e2e` entry); GitHub's branch-rules evaluation for `main` lists both. Threshold met first: three consecutive normal pull requests with `e2e` green (#19, #20, #21). Final governance: pull request required → `verify` + `e2e` required → merge commits only → 0 approving reviews, review-thread resolution, deletion / force-push blocked, Repository Admin bypass PR-only. Validated on this record's own pull request (#22) without issuing a merge command while blocked, sampled every 3 seconds: `BLOCKED` one second after the PR opened (19:58:43, no check reported yet); `BLOCKED` with `verify` in progress (19:58:53); **still `BLOCKED` with `verify` SUCCESS while `e2e` was queued (19:59:12) and then in progress (19:59:16)** — the newly required `e2e` gate holds on its own; `CLEAN` only once `verify` and `e2e` had both passed (20:00:07). `preview` / `deploy` skipped throughout.
- **19:50 — Artifact Actions Node 24 maintenance COMPLETE** — PR #21 merged with a merge commit (`dd4b3c1`; `verify` + `e2e` green, no bypass): `actions/upload-artifact` v5.0.0 (`330a01c`, node20) → v7.0.1 (`043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`, node24) in `verify` and the failure-only Playwright report step; `actions/download-artifact` v5.0.0 (`634f93c`, node20) → v8.0.1 (`3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c`, node24) in `e2e`, `preview` and `deploy`; `actions/checkout` v5.1.0 and `actions/setup-node` v5.0.0 already declare node24 and were left alone; exactly five pin lines changed, no workflow logic. Release notes read for both: upload v7's new `archive` input defaults to `true` (zipped upload unchanged); download v8 defaults `digest-mismatch` to `error` — accepted as fail-closed hardening (a corrupted `dist` must not reach `e2e`, Preview or deploy). Read back: the two Node 20 runtime warnings of the previous `main` run are gone (0 on the PR run and on `main` run 35995392566); `verify` uploaded `dist-<sha>` (24 files) and `e2e` downloaded it with a verified digest; Playwright 20 passed / 2 skipped; `preview` / `deploy` skipped. `e2e`-green pull requests: 3/3 (#19, #20, #21) — promotion threshold reached.
- **19:07 — Portfolio `main` ruleset applied** — `protect-main` (ruleset 23934021) created through the API after M1's `verify` had run green on `main`: pull request required (0 approving reviews, review-thread resolution), required status check `verify` (`strict: false`), deletion and force-push blocked, merge commits only, bypass = Repository Admin role in pull-request mode only. Read back in full and diffed against the intended payload: identical apart from two GitHub-supplied defaults (`required_reviewers: []`; `require_extra_approval_for_unattributed_changes: true` — the same default present on the five solo-maintained repository rulesets, which merged 0-review PRs earlier today). Validated on this record's own pull request (#20) without issuing a merge command while blocked: `mergeStateStatus` `BLOCKED` one second after the PR opened (19:11:32, `mergeable: MERGEABLE`, no check reported yet, no review requested), still `BLOCKED` at 19:12:02 with the required `verify` in progress, `CLEAN` at 19:13:49 once `verify` and `e2e` had passed (`preview` / `deploy` skipped). `e2e` green on #20 → `e2e`-green pull requests toward promotion: 2/3 (#19, #20).
- **18:56 — M1 COMPLETE (technically deployable)** — PR #19 merged with a merge commit (`4258718`; `verify` green, `e2e` green, no bypass; `preview` / `deploy` skipped — dormant behind the repository variable `CLOUDFLARE_DEPLOY_ENABLED`); `verify` + `e2e` green again on `main` (run 35990123473). Landed: Node ≥ 22.12 (`engines`, `.nvmrc`); Wrangler 4 and the `wrangler.toml` production contract (`not_found_handling = "404-page"`, `html_handling = "auto-trailing-slash"`, `workers_dev`, `preview_urls`, empty `previews` block for Worker Previews); Astro `trailingSlash: 'always'`; the route-state policy `src/data/routes.mjs` (single source for placeholder / maintenance / 404 states, shared by the sitemap filter, `BaseLayout`, `robots.txt` and the dist gate); typed `PageMeta` in `BaseLayout` with the policy as the robots floor, titles `<Page> | Eric So`, no canonical on 404, `apple-touch-icon`; `PlaceholderPage` (In Preparation: 200 + `noindex, nofollow`) for `/about/`, `/internal-builds/`, `/writing/`, `/topics/`; custom 404 (`ROUTE_NOT_FOUND`, real 404 status, `noindex`); `/under-maintenance/` chromeless + `noindex, nofollow`; `robots.txt` endpoint (AI-crawler policy from `docs/DECISIONS.md`; `Sitemap:` only when `PUBLIC_SITE_INDEXABLE=true`); sitemap always built, live routes only; `scripts/verify-dist.mjs` (zero dependencies, 547 checks in both modes); Playwright smoke (desktop + Pixel 7) against `wrangler dev`; `ci.yml` (`verify` → `e2e` → dormant `preview` / `deploy`, actions pinned to commit SHAs) and `preview-cleanup.yml`. Owner review adjustments applied before push: empty `previews = { }`; no production deploy assumed before the first Worker Preview; `e2e` a normal, red-capable job that the ruleset does not require yet; Preview URL parsed with `jq -er '.preview_urls[0] // .preview.urls[0]'`. Read back from the `main` CI artifact: 404 `noindex` without canonical; placeholders and maintenance `noindex, nofollow`; every page `noindex, nofollow` pre-launch; sitemap = exactly the 9 live routes; `robots.txt` = the five policy blocks, no `Sitemap:` line. The superseded local branch `docs/deploy-readiness-proposal` was discarded (the owner's roadmap and M1 replaced it).
- **~17:51 — StarLedger Template sync COMPLETE; the StarLedger / Template cycle is closed** — starledger-template PR #5 merged with a merge commit (`ff93cbe`): regenerated from `starledger@e4ec5b7` with `pnpm template:build --force --verify` (install / typecheck / test / build / `setup:doctor --template-clean` all green); generated output vs the template's `main`: added 0 · removed 0 · changed 1 (`README.md`, the verbatim `README.template.md`) · unchanged 366; the template's `verify`, `verify-agent-artifacts` and `verify-ai-provenance` checks green on the PR. Read back: the template's `README.md` on `main` is byte-identical to the parent's `README.template.md@e4ec5b7` — H1 `StarLedger Template`, template value line, ACTIVE + Apache-2.0, no child-release version badge (D18).
- **~17:34 — StarLedger Step 5 parent cycle COMPLETE** — starledger PR #290 merged with a merge commit (`e4ec5b7`; required `verify`, `verify-agent-artifacts`, `verify-ai-provenance` and the triggered template-smoke `build-and-check` all green; no bypass). `README.md`: bold value proposition (the repository description), status · static exact-version `v1.3.0-alpha.1` badge linked to that Release · Apache-2.0 badge, "Live dashboard →" link, a three-sentence business-readable summary, five evidence-backed highlights, then Quick start; the phase table and contracts line moved unchanged into `Phases and contracts` immediately below Quick start; nothing version- or identifier-shaped above Quick start except the badge. `README.template.md`: H1 `StarLedger Template`, the template repository's description as value line, Apache-2.0 badge beside the status badge — deliberately no child-release version badge (a parent-maintained file must not carry the template repository's release state). No release for this cycle. Read back from `main` by an unauthenticated client: all eleven confirmation points pass. The template repository is regenerated from `e4ec5b7` in the next, separate cycle.
- **~17:10 — Worldview Council Step 5 cycle COMPLETE** — religion-council PR #64 merged with a merge commit (`e50b51a`; required `test (3.9)` / `test (3.11)` green, branch up to date, no bypass). README top fold made business-readable: the v0.5 → v0.13.1 narrative replaced by a version-independent current-state summary (three execution modes; the opt-in strict path checks structured claims against curated sources and refuses unverifiable quotations — fails closed; one versioned, conformance-tested retrieval contract; default retriever lightweight and file-based, no vector or network RAG backend adopted) plus a `CHANGELOG.md` link; `Why it's different` condensed from seven to five evidence-backed bullets, the forward-looking "Built for a RAG future" bullet replaced by "One retrieval contract, multiple implementations"; the strict-finalization subsections moved below Quick start under `Strict mode (opt-in)` / `嚴格模式 (opt-in)`; implementation detail (identifiers, thresholds, ADR references) moved below the fold in the project's existing wording. EN and 繁體中文 halves mirror. No release (D4). Read back from `main` by an unauthenticated client: five bullets in both halves, Quick start above the detail sections, no version narrative / identifiers / thresholds / ADR references above Quick start, CHANGELOG link resolves; push CI green on `e50b51a`.
- **~16:30 — Step 5 decisions recorded (D11–D17); standard and issue #1 aligned** — the owner's decisions on the Step 5 proposal recorded as D11–D17 in the review record (AI Arena: MIT code + contribution/content policy with the revised rights / removal / confidentiality wording, `v0.1.0` as a normal release, `zh-Hant` filenames; Worldview Council: version-independent current-state summary only; StarLedger: static `v1.3.0-alpha.1` badge; homebrew-tap: BSD-2-Clause with the app's AGPL-3.0 stated; portfolio: code MIT + original content All Rights Reserved via `NOTICE.md`; bilingual filenames `zh-Hant` with the 繁體中文（香港） label kept; forks reworded + archived — a public fork cannot be made private in place); standard §3 / §5 / §9 / §10 updated; issue #1 Step 5 rewritten in execution order. Facts verified first: GitHub's fork-visibility rule; shields' dynamic release badge renders "no releases" for StarLedger. Owner adjustments before execution: `LICENSE` holder Eric So; two PR-template checkboxes; D11–D17 kept out of the historical review sections, added as a "Post-review implementation decisions" addendum.
- **~16:25 — AI Arena Step 5 cycle COMPLETE — `v0.1.0`** — PR #28 squash-merged at 16:20 (`621975f`; required `validate` green, no bypass): MIT `LICENSE` (holder Eric So), contribution/content policy in `CONTRIBUTING.md` + `CONTRIBUTING.zh-Hant.md` with two PR-template confirmation checkboxes, `README.zh-TW.md` / `CONTRIBUTING.zh-TW.md` → `zh-Hant` (every reference updated), README top fold EN + zh-Hant (value line, status / version / license badges, live gallery link, highlights condensed from existing evidence, `Daily Workflow` kept as quick start) and a License section scoping MIT to the code. Annotated tag `v0.1.0` on the squash commit; normal GitHub Release published (first tag of the repository). Read back: tag object → `621975f`; Release not a pre-release and reported as latest; GitHub licence detection `mit`; `main` CI validate + deploy green on `621975f`; live site 200; new files 200 and old `zh-TW` paths 404 from an unauthenticated client.
- **~05:15 — Step 4 housekeeping** — the one known residue of the template-generation contract closed at the source: starledger #288 merged (`b12b94e`) — `deploy-freshness.yml` joins `NEUTRALIZE_SCHEDULE_WORKFLOWS` (emitted dispatch-only; on a fresh template it could only alarm, daily, with no committed dataset and no Pages site yet), and the builder now fails if any emitted workflow still carries a live `schedule:` trigger (6 new tests; parent CI incl. the template self-test green). starledger-template #4 merged (`094b271`): regenerated from `starledger@b12b94e` with `--verify` green — 10 changed paths, 0 added / removed, all inside the builder allowlist; read back from `main`, none of the 9 shipped workflows fires on its own. Also corrected here: the Step 4 COMPLETE entry's time (~04:30, not 05:30 — PR #14 merged 20:29Z) and the Output summary line (steps 2–4 complete).
- **~04:30 — Step 4 COMPLETE** — (A) starledger #287 merged: the template builder never emits parent-only workflows (`EXCLUDE_WORKFLOWS`) and omits `# template-builder: omit` steps from `ci.yml` (fail-closed transform, 8 new tests; parent CI incl. the template self-test green). (B) starledger-template #3 merged (`7cf81f5`): regenerated from `starledger@44065b5` with `--verify` green, two commits (content sync; `.github` alignment), its CI green for the first time since generation; #1/#2 closed as superseded. (C) ai-pet-usage: `swift-tests` runs on every PR (#104), stable context name (#105), sole required status check (`strict: false`), validated by #106 (blocked while pending, merged without approval once green). Eight Tier-A lifecycle badges read back from `main`: 8/8.
- **~03:55 — Step 4b 7/8; rulesets aligned** — the five solo-maintained repos' rulesets set to PR + required CI, 0 approving reviews (`ai-arena` also `require_last_push_approval` off); every other rule and the bypass list untouched, verified by full ruleset read-back diffs. Badge PRs then merged without bypass: ai-pet-usage #103, skills #248, religion-council #63 (merge), tailscale-ai-egress #35, ai-arena #27 (squash — linear history). starledger-template: the formatting PR #2 exposed that its CI is structurally broken (inherited workflows reference `scripts/smoke-notifier-*.mjs` and `README.template.md`, which the sanitized template does not contain) — both PRs held pending a decision.
- **~03:15 — Step 4a COMPLETE; 4b partial** — descriptions + topics applied on all 9 public repos via `gh repo edit` (sequential; before/after captured; API read-back: descriptions exact, topics set-equal; visibility/homepage/default branch/license untouched). Status-badge PRs opened on all 8 Tier-A repos: merged portfolio #11 (BUILDING) and starledger #286 (ACTIVE); blocked by review rulesets with CI green — ai-pet-usage #103, skills #248, tailscale-ai-egress #35, religion-council #63, ai-arena #27; starledger-template #1 blocked by a pre-existing `prettier --check` failure in two issue-template YAML files (the `CI` workflow has failed on `main` since 2026-06-24).
- **~02:40 — Step 3 COMPLETE** — `skills` badge/`checks.py` PR #247 merged by owner bypass (explicit exception recorded on the PR; required CI checks green on the PR and again on the merge commit `df1248e`); annotated tag `v0.1.16` on the merge commit; GitHub pre-release published with notes covering the evolution since `alpha-0.1.2` without inventing intermediate tags.
- **~02:30 — Step 3 B/C done** — StarLedger `v1.3.0-alpha.1` pre-release cut (Discovery Inbox, P5) and the `v1.2.0-alpha.1` title clarified ("Reusable template (P4) alpha", body unchanged); StarLedger Template `v1.2.0-alpha.1` pre-release cut; `skills` badge/`checks.py` PR #247 opened (CI green; awaiting the ruleset's required review before tag `v0.1.16` + pre-release).
- **~02:10 — `tradingview-mcp` made private** and removed from public-repository / portfolio scope: Lab entry deleted, standard's derivative tier removed as empty (forks are now Tier B), issue #1 items removed; the review record keeps its historical findings with a post-review note.
- **~02:00 — Step 2 COMPLETE** — portfolio PR #6 (`6cee668`: README wording, ROADMAP state, Opus Pack journey chronology with evidence pointers, AI Pet Usage in Lab, `opus-pack` → `skills` rename record, deploy-readiness decisions) and the `tradingview-mcp` README truth-fix PR #28 (`41ff043`) merged.
- **~01:20 — Issue #1 re-based** — body replaced by a six-step implementation checklist that references the standard by section; the original 2026-09-23 body is preserved in the review record's Appendix A.
- **01:09 — PR #5 merged** (`cc9cb86`) — [`docs/PUBLIC_REPO_STANDARD.md`](docs/PUBLIC_REPO_STANDARD.md) v1 (tiers, description, top fold, topics, releases with the `ai-pet-usage` governed exception, five-state status, integrity, licensing state) + [`docs/reviews/2026-09-24-public-repo-metadata-review.md`](docs/reviews/2026-09-24-public-repo-metadata-review.md) + DECISIONS entries.
- **~00:00–00:50 — Public-repo metadata review** — three independent Sonnet 5 read-only gates (metadata audit, integrity critic, release-tooling trace); every material finding reproduced first-hand and GitHub state verified unchanged afterwards. Found: issue #1 had drifted from the consolidated standard; the `ai-pet-usage` tag scheme is a runtime/distribution contract (updater, release workflow, Homebrew cask); the Opus Pack journey row was the review's illustrative example verbatim.

### 2026-09-23

- **23:48 — PR #4 merged** (`096a4e9`) — Grok 4.6 fixes ×2 + ROADMAP/README; PR #2 closed as merged in the same merge (its branch was the base of `review/grok-4.6`).
- **23:45 — Grok 4.6 round-2 fixes committed** (`e4de03a`) — header-wrap regression fixed (desktop header trimmed to the 5 primary sections; Soon routes in footer + mobile menu), evidence honesty (only reachable evidence shown; a placeholder note when none), footer Soon badges, boot a11y (page inert while the overlay is up), hero alias footnote dropped.
- **~23:30 — Grok 4.6 round 2** (`xcheck --grok`) — validated round 1 and caught a regression (the longer brand re-opened the nav wrap) plus incomplete round-1 fixes.
- **23:15 — Grok 4.6 round-1 fixes committed** (`8eee0d9`) — Topics marked Soon, boot FOUC fixed (localStorage skip before paint), Lab-teaser WCAG + StatusBadge, evidence de-duplication, brand positioning, mobile build-stream dots + pet/footer clearance.
- **~22:50 — Grok 4.6 round 1** (`xcheck --grok`) — product / IA / content / design review; 7 findings (no high-severity).
- **21:15 — Roadmap published.** This file added and merged to `main`.
- **20:43 — PR #2 opened** — [Phase 1 cross-model review fixes](https://github.com/F-e-u-e-r/portfolio/pull/2) (`review/phase1-cross-model` → `main`); merged 23:48 together with PR #4.
- **20:42 — Review record committed** (`fbaf940`) — `docs/reviews/2026-09-23-phase1-cross-model.md` (lands with PR #2).
- **20:36 — Review fixes committed** (`a0934c3`) — 11 confirmed xcheck findings fixed; `astro build` (15 pages) + `astro check` (0 errors) green; header re-rendered at 960px; OG image 1200×630 generated.
- **20:00–20:35 — xcheck (cross-model review)** of Phase 1 by `gpt-5.6-sol` + `gpt-5.6-luna` (max effort). Round 1 complete (11 findings, no high-severity); each reproduced before fixing. Round 2 re-review quota-blocked (see xcheck output).
- **19:56 — Issue #1 opened** — [public-repo README / metadata cleanup](https://github.com/F-e-u-e-r/portfolio/issues/1): 9 public repos benchmarked against the `ai-pet-usage` gold standard.
- **19:54 — Phase 1 committed + pushed to `main`** (`386efbd`) — foundation + homepage shell.
- **~19:42 — Phase 1 build first green** — `astro build` 15 pages, `astro check` 0 errors.
- **17:51–19:40 — Phase 1 built** — Astro 7 static scaffold, design system, typed content schema, full IA routing, Matrix boot, Journey + Applied AI Build Stream, case-study template, maintenance/404, Cloudflare config; the public-repo README review ran in parallel (3 subagents).

---

## Output summary

**Phase 1 — foundation + homepage shell** (on `main`, `386efbd`)
- 15 static pages: Home, Case Studies (+4 dynamic cases), Journey, Lab, Internal Builds, Writing, Topics, About, Contact, `/under-maintenance`, 404.
- Typed content schema (§35): `cases` + `lab` collections (status, capabilities, evidence, milestones).
- Design system: dark-first tokens, one Matrix-green accent (§36, 80/15/5), restrained motion (§37).
- Matrix boot (§5), desktop pet (§14), unified status system (§38), Cloudflare Workers Static Assets config (§34).
- Gates: `astro build` → 15 pages · `astro check` → 0 errors, 0 warnings.

**Public-repo README review** → [Issue #1](https://github.com/F-e-u-e-r/portfolio/issues/1)
- 9 non-fork public repos audited vs the `ai-pet-usage` gold standard: per-repo issues + paste-ready descriptions + cross-cutting fixes + a house README template.
- P0: `tradingview-mcp` README identity bug (title + clone URLs point at the wrong repo `tradingview-mcp-review`; opening says 9 tools, ships 7); `opus-pack` version mismatch (README `alpha-0.1.16` vs latest tag `alpha-0.1.2`); `religion-council` 8 release titles still "Religion Council".
- Superseded 2026-09-24: issue #1 re-based onto `docs/PUBLIC_REPO_STANDARD.md`. The `opus-pack` "version mismatch" is a by-design decoupling (version of record is `plugin.json` `0.1.16`) — the fix is a canonical `v0.1.16` release, not a badge edit; the `religion-council` titles stay (historical names).

**Cross-model review fixes** → [PR #2](https://github.com/F-e-u-e-r/portfolio/pull/2) (merged 2026-09-23 23:48 via PR #4)
- 20 files, +94 / −31.

**Public-repo metadata standard** → [PR #5](https://github.com/F-e-u-e-r/portfolio/pull/5) (merged 2026-09-24 01:09)
- `docs/PUBLIC_REPO_STANDARD.md` v1 + review record (findings F1–F11, Opus Pack chronology, decisions D1–D10); issue #1 is the implementation checklist — steps 2–4 complete, steps 5–6 open.

**M1 — production baseline** → [PR #19](https://github.com/F-e-u-e-r/portfolio/pull/19) (merged 2026-09-24 18:56, `4258718`)
- 25 files, +1807 / −148 (1035 of them `package-lock.json`): route-state policy, page states, `robots.txt` + sitemap, dist gate, Playwright smoke, CI (`verify`, `e2e`, dormant `preview` / `deploy`), Worker Previews config.
- Gates: `astro check` 0 errors · `astro build` 15 pages · `verify-dist` 547/547 in both modes · Playwright 20 passed / 2 skipped · CI `verify` + `e2e` green on the PR and on `main`.

**M2 — automation hardening** → [PR #23](https://github.com/F-e-u-e-r/portfolio/pull/23) (merged 2026-09-25 01:55, `a44267e`)
- 9 files, +730 / −10: two Cloudflare gates (`CLOUDFLARE_PREVIEW_ENABLED` / `CLOUDFLARE_DEPLOY_ENABLED`), fail-closed Preview URL parser with an exact-hostname pin, Preview smoke (navigation-only custom 404, optional Access service token), 25 tool tests in the required `verify`, DECISIONS entries. Preceded by the M2 controlled Preview (2026-09-25 00:42–00:57): an assets-only Worker Preview works without a script; the Worker `ccso-portfolio` exists with no production deployment.
- Gates: `test:tools` 25/25 · `astro check` 0 errors · `verify-dist` 547/547 · Playwright 20 passed / 2 skipped · CI `verify` + `e2e` green on the PR and on `main`.

**Homepage simplification** → [PR #25](https://github.com/F-e-u-e-r/portfolio/pull/25) (merged 2026-09-25 13:30, `121c1df`)
- 3 files, +31 / −44: the About card, the "get in touch" CTA and the shared footer's contact card removed; footer = one-line identity + alias note + real contact links only (GitHub / Feuer · Email; LinkedIn once set) + navigation + legal; DECISIONS entry. The homepage ends on the Lab / Writing teaser.
- Gates: `astro check` 0 errors · `astro build` 15 pages · `verify-dist` 547/547 · `test:tools` 25/25 · Playwright 20 passed / 2 skipped · CI `verify` + `e2e` green on the PR and on `main`.

**M2 — account side, automated Preview validation, hold** → [PR #27](https://github.com/F-e-u-e-r/portfolio/pull/27) (merged 2026-09-25 14:39, `3f58db8`) + the M2 Preview hold pull request (`preview_urls = false`, DECISIONS entry)
- Validated: Cloudflare Access "Previews only" + service token (Phase A, 12:27) · Worker-scoped Editor token create / update / delete (Phase B, 12:43–12:46) · GitHub secrets + variables (14:20) · automated `pr-27` Preview on PR #27: create → exact-hostname pin → smoke 11/11 → PR comment, twice (update in place) · cleanup control-plane deletion (14:40).
- OPEN (provider): the deleted `pr-27` alias kept routing after the deletion and after Preview URLs were disabled → [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73). Automated Preview URLs disabled (`CLOUDFLARE_PREVIEW_ENABLED` removed 17:06; `preview_urls = false` in `wrangler.toml`). M2 not complete; launch path unblocked → M2.5.

**M2.5 — `workers.dev` production** → [PR #29](https://github.com/F-e-u-e-r/portfolio/pull/29) (merged 2026-09-25 22:30, `3c6a6a7`) + [PR #30](https://github.com/F-e-u-e-r/portfolio/pull/30) (merged 23:33, `12de99c`) — technical gate met 23:35 · completion record landed 23:50 ([PR #31](https://github.com/F-e-u-e-r/portfolio/pull/31), `10cd55e`)
- Production live on `https://ccso-portfolio.secc-biz.workers.dev` (pre-launch: `noindex, nofollow`, canonical `https://ccso.shsl.world/`, `PUBLIC_SITE_INDEXABLE` false / unset); repository variables `PRODUCTION_URL` + `CLOUDFLARE_DEPLOY_ENABLED=true` (22:25); production deploys on every push to `main` under `verify` → `e2e` → `deploy` → bounded readiness → `scripts/smoke-preview.sh` (PR #30: `needs: [verify, e2e]`, the inline `curl | grep -q` smoke replaced).
- Gates: initial acceptance 64/64 from the verified artifact (22:42) · final `main` run 36154921374 fully green (verify → e2e → deploy; Deploy SUCCESS, readiness 1/24 → 200, smoke 11/11) · Cloudflare `{enabled:true, previews_enabled:false}`, deployments / `deployed_on` populated · `CLOUDFLARE_PREVIEW_ENABLED` absent · no DNS / custom-domain change. The M2 Preview cleanup issue #73 stays OPEN, separately.

---

## xcheck output (cross-model review of Phase 1)

**Reviewers:** `gpt-5.6-sol` + `gpt-5.6-luna` (`model_reasoning_effort=max`, via `codex exec`).
**Round 1:** complete. No high-severity defects. **11 unique findings** (4 raised by both), each reproduced before fixing:

| # | Sev | Finding | By | Status |
|---|-----|---------|----|--------|
| 1 | Med | `--c-fg-subtle` below WCAG AA (~3.9:1) | both | Fixed → `#828f88` (≥4.5:1 all surfaces) |
| 2 | Med | Desktop header nav wraps into two rows at ~901–999px | sol | Fixed → disclosure-menu breakpoint 900 → 1024px |
| 3 | Med | `tsconfig baseUrl` deprecated → `tsc` TS5101 | sol | Fixed → removed unused `baseUrl` + `paths` |
| 4 | Low | No `aria-current`; mobile links lack active state | both | Fixed |
| 5 | Low | Listing pages skip a heading level (h1 → h3) | sol | Fixed → `CaseCard` `h2` on listings; Lab `h2` |
| 6 | Low | Build-stream milestones not exposed to screen readers | luna | Fixed → `sr-only` milestone text |
| 7 | Low | Boot overlay fades (420ms) on repeat visits | luna | Fixed → instant removal |
| 8 | Low | `summary_large_image` but no `og:image` | both | Fixed → `public/og.png` (1200×630) + tags |
| 9 | Low | All 9 section pages share one meta description | sol | Fixed → per-page descriptions |
| 10 | Low | `prefetch` config inert (no link opts in) | both | Fixed → `prefetchAll: true` |
| 11 | Low | Maintenance page has no `h1` | luna | Fixed |

**Not a defect:** both reviewers' sandboxed `astro check` / `tsc --noEmit` failed only because the review sandbox was read-only (could not write `.astro/`); both gates pass cleanly in the normal environment.

**Round 2 (re-review of the fix diff):** **quota-blocked** — the OpenAI/Codex account hit its usage limit mid-run (resets **2026-09-24 ~01:00 UTC+8**), so no PROCEED/FIX verdict was returned; **none is inferred**. Round-1 fixes stand on execution-based verification (build, check, contrast recomputation, 960px header render). Re-run available after the quota resets.

> Reviewer-family note: `sol` and `luna` are two variants of one provider family (OpenAI GPT-5.6); both differ from the author family (Claude Opus 4.8), so this is a cross-family gate on the author, not two independent families.

### Grok 4.6 (high) — 2 rounds

Grok 4.6 (high) ran twice via `xcheck --grok`, each on two packets (the prior GPT review + PR #2 as context; and the site's structure / content / design). Grok is a live principal (not OS-sandboxed), so it ran review-only on a clean tree — the tree was verified untouched after each round. Grok found deeper product / IA / design issues than the GPT pass.

**Round 1** — 7 findings (no high-severity): nav stubs, boot FOUC (deferred-module flash + per-tab replay), unlinked content graphs, evidence-in-template, positioning, homepage density, Worldview repo name. Applied: Topics Soon, blocking head-script boot skip + localStorage, Lab-teaser StatusBadge, evidence de-dup, brand, mobile dots + pet clearance.

**Round 2** — validated round 1 and **caught a regression**: the longer brand + Soon pills re-opened the header wrap (~1025–1090px, measured). Applied: desktop header trimmed to 5 primary sections (Soon routes → footer + mobile menu; no wrap 861–1101px+); evidence honesty completed on cards + case pages (empty-state note); footer Soon badges; first-visit boot made keyboard/AT-safe (rest of page `inert`); hero alias dropped.

**Deferred with reasons** (not fixed this round): content-graph refactor (buildStream ↔ collections, lab detail pages) vs frozen scope §10/§27; journey roles / About answered fields / more transformation cases (need real career content, MVP pass); 10-section homepage (frozen §4); `religion-council` rename (held issue #1); web fonts + robots/sitemap/JSON-LD (at deploy).

---

## Next

- **Step 2 — COMPLETE.** `portfolio` PR #6 and the TradingView README truth-fix PR merged; `tradingview-mcp` made private and removed from public-repository / portfolio scope.
- **Step 3 — COMPLETE.** A: `skills` `v0.1.16` pre-release (tag on merge commit `df1248e`); B/C: StarLedger `v1.3.0-alpha.1` pre-release + `v1.2.0-alpha.1` title clarified, StarLedger Template `v1.2.0-alpha.1` pre-release; D/E: no action by design (religion-council's next genuine release carries "Worldview Council"; ai-pet-usage stays on its governed exception — migration ticket when that work is scheduled).
- **Step 4 — COMPLETE.** 4a descriptions + topics on all 9 public repos; 4b lifecycle badges on all 8 Tier-A repos (8/8 read back from `main`); solo-maintained rulesets = PR + required CI, 0 approving reviews; `ai-pet-usage` gained its required check; the StarLedger template builder repaired at the source and the template resynchronised.
- **Step 5 — repository track, now parallel to the launch critical path.** Decisions frozen (D11–D18). Done: ai-arena ✓ (`v0.1.0`, PR #28) · religion-council ✓ (PR #64) · starledger ✓ (PR #290, `e4ec5b7`) · starledger-template ✓ (PR #5, `ff93cbe`). Remaining as a parallel repository-hygiene track (owner roadmap, 2026-09-24): portfolio / homebrew-tap licensing → bilingual filenames → forks → step 6 extensions. One repository per cycle: diff proposal → PR → CI → merge → release.
- **Launch critical path (owner roadmap, 2026-09-24):** Deploy Readiness Audit ✓ → CI + test baseline ✓ → page states (custom 404 · In Preparation placeholder · Under Maintenance) ✓ → SEO metadata / robots / sitemap ✓ (**M1 COMPLETE**, PR #19, 18:56) → `main` ruleset ✓ (19:07) → artifact actions pinned to Node 24 ✓ (PR #21, 19:50) → `e2e` required ✓ (19:56 — CI governance closed: pull request → `verify` + `e2e` required → merge commits only → 0 approving reviews) → M2 controlled Worker Preview ✓ (2026-09-25 00:42–00:57: assets-only Preview without a script; Worker `ccso-portfolio` exists, no production deployment) → M2 automation hardening ✓ (PR #23, 01:55: `CLOUDFLARE_PREVIEW_ENABLED` / `CLOUDFLARE_DEPLOY_ENABLED` split, fail-closed parser + hostname pin, Preview smoke, `test:tools` in `verify`) → M2 account side ✓ (2026-09-25: Access "Previews only" + Phase A 12:27, Editor token Phase B 12:43–12:46, GitHub secrets + variables 14:20) → automated Preview validation PR #27 ✓ (create / exact-host pin / smoke / comment green twice; cleanup control-plane deletion green, 14:40) → **M2 cleanup / routing provider issue OPEN** (the deleted `pr-27` alias kept routing after the deletion and after Preview URLs were disabled — [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73); automated Preview URLs disabled: `CLOUDFLARE_PREVIEW_ENABLED` removed 17:06, `preview_urls = false` in `wrangler.toml`; **M2 not complete**, launch path unblocked) → M2.5 `workers.dev` production ✓ (2026-09-25: PR #29 first deploy 22:31, acceptance 64/64 22:42, PR #30 smoke hardening 23:33, `main` run 36154921374 fully green — technical gate met 23:35 · completion record landed 23:50 (PR #31); verify → e2e → deploy → readiness → production smoke 11/11; subdomain `{enabled:true, previews_enabled:false}`; **M2.5 COMPLETE**) → **next: M3 — `ccso.shsl.world` DNS migration + custom domain (separate AUTH; `secc.studio` fully deferred: no DNS change, no redirect)** → content (About, cases, Journey) → Writing → Topics → launch polish. Milestones M1 technically deployable ✓ → M2 / M2.5 pre-launch production ✓ (Preview automation on hold) → M3 public MVP → M4 full v1. `e2e`-green pull requests: 3/3 (#19, #20, #21); `e2e` required since 19:56.
- Re-run xcheck round 2 after the quota resets.
- MVP content pass (Phase 2): real RAG + Sales-to-Ops case content, journey milestones, About, LinkedIn + resume, web fonts.
- Deployed to Cloudflare `workers.dev` (M2.5 ✓; automated Worker Previews on hold pending [cloudflare/developer-platform#73](https://github.com/cloudflare/developer-platform/issues/73)); `ccso.shsl.world` at M3. The deploy-readiness items from `docs/DECISIONS.md` (`robots.txt` + sitemap, `noindex` on maintenance / placeholder pages, AI-crawler policy) landed in M1; `PUBLIC_SITE_INDEXABLE` stays `false` until M3.
