# Cloudflare Workers Static Assets (§34) — serves the static Astro build in dist/.
# Deploy: `npm run build` then `npx wrangler deploy` (requires `wrangler login`).
name = "ccso-portfolio"
compatibility_date = "2026-09-01"

[assets]
directory = "./dist"
not_found_handling = "404-page"
