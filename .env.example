# Pre-launch default. Set to "true" only at M3 (Public MVP): live routes become index, follow and robots.txt
# advertises the sitemap. Placeholder / maintenance / 404 pages stay noindex regardless of this flag.
# In CI the value comes from the repository variable PUBLIC_SITE_INDEXABLE (unset = false).
PUBLIC_SITE_INDEXABLE=false
